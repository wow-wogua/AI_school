# 交付文档 —— 教师学生数智化成长平台（项目二）

> 交付日期：2026-08-20 ｜ 状态：功能全量完成，回归 10/10 PASS（服务端 160 断言 + 前端 E2E 94 断言）

## 一、项目概述

围绕「**采集数据 → 形成成长档案 → AI 分析 → 教师干预 → 生成成长报告**」建设的初中素质报告平台。教师日常录入成绩与过程性评价，系统自动聚合九格综合素质、时间轴、活动、荣誉与综评，AI 生成学业分析与班主任寄语，最终一键渲染 50 页 1:1 复刻纸质模板的《学生成长报告册》PDF（支持单生 / 全班 / 全年级批量）。

### 功能范围（对照 功能点.md 第二部分）

| 功能点 | 状态 |
|---|---|
| §1 用户与权限（管理员/班主任/任课教师三级，班级数据隔离 + 越权 403） | ✅ |
| §2 学生档案（基本信息 + 照片 + 成长信息聚合） | ✅ |
| §3 成绩管理（录入 / Excel 导入 / 排名 / 趋势·雷达·对比） | ✅ |
| §4 AI 学业分析（优势/待提升学科、变化趋势、异常提醒） | ✅ |
| §5 过程性评价（选指标 ±分 + 备注，评价同时写穿九格） | ✅ |
| §6 九格综合素质评价（九维 × 二级指标，指标体系可管理） | ✅ |
| §7 九格学生画像（九维雷达：本学期/上学期/班级/年级，PDF 内呈现） | ✅ |
| §8 学生事件时间轴 | ✅ |
| §9 活动管理（创建/报名/签到/表现评价 → 自动入档案） | ✅ |
| §10 荣誉与证书 | ✅ |
| §11 综合素质评定（五维 A/B/C/D + 自动综合） | ✅ |
| §12 班主任寄语（AI 生成草稿 → 修改 → 保存） | ✅ |
| §13 AI 学生成长总结（亮点/学习发展/素质发展/建议四块） | ✅ |
| §14 学生自评 / §15 家长寄语 | 裁剪（需求方明确不做） |
| §16 一键生成成长报告（单生/全班/全年级，下载/打印/归档） | ✅ |

## 二、技术栈与模块

| 模块 | 技术 | 端口 |
|---|---|---|
| `report-server` | Spring Boot 3.3.5 / Java 21 / MyBatis-Plus | 8080 |
| `report-web` | Vue 3 + Element Plus + Vite（代理 /api → 8080） | 5173 |
| `report-renderer` | Thymeleaf + ECharts 5.6 (svg) + Playwright Chromium，模板图片 data-URI 内联；每次渲染 fork 独立 JVM，改模板只需重新 package，服务端免重启 | — |
| `deploy` | docker-compose：MySQL 8.0 / Redis 7 / MinIO（报告 PDF 存 MinIO，桶 `ai-school-reports` 服务端自建） | 3306 / 6379 / 9000(控制台 9001) |
| `tools` | apache-maven-3.9.16、m2-repo、npm-cache（离线构建缓存） | — |

数据卷：`mysql-data` / `redis-data` / `minio-data`（停容器不丢数据；删卷 = 清空全部数据）。

## 三、环境要求

- JDK 21、Node 18+、Docker Desktop
- Maven 用 `tools/apache-maven-3.9.16`，**每个模块目录内带 `settings.xml`，构建命令必须加 `-s settings.xml`**
- 回归脚本需 Python：`D:/env/python/python3.12.8/python.exe`；E2E 需系统 Chrome

## 四、启动步骤

### 首次初始化（新机器）

```bash
cd deploy
docker compose up -d                                    # 三容器起来
docker exec -i aischool-mysql mysql -uroot -paischool123 ai_school < schema.sql
docker exec -i aischool-mysql mysql -uroot -paischool123 ai_school < seed.sql
cd ../report-renderer && mvn -s settings.xml package -DskipTests   # 渲染 classpath（target/classes + target/lib）
```

### 日常演示

```bash
docker start aischool-mysql aischool-redis aischool-minio     # 或 Docker Desktop 里启动
cd report-server && mvn -s settings.xml spring-boot:run        # 后端 8080
cd report-web && npm run dev                                   # 前端 5173
```

浏览器访问 http://localhost:5173 。

## 五、演示账号与种子数据

| 账号 | 密码 | 角色 |
|---|---|---|
| admin | admin123 | 学校管理员（全量） |
| litao | aischool123 | 班主任·初一(1)班 |
| zhaolaoshi | aischool123 | 班主任·初一(2)班 |
| wanglaoshi | aischool123 | 任课教师·初一(2)班 |

- 初一(1)班 50 人（学生1 为契约基线，数据最全）；初一(2)班 2 人（51 洪雨欣 / 52 聂远航）——**演示中随意编辑/生成请用 class2，别动 class1**
- 学期：termId=2（2026 春季）为默认演示学期
- MySQL root/aischool123（库 ai_school）；MinIO aischool/aischool123

## 六、验证与回归

一键全链（需后端 + 前端 + 三容器都在跑）：

```bash
bash report-server/scripts/run_regression.sh
```

10 个脚本顺序执行：contract（数据契约 50 页）、rbac 18、ai 11、retry 4（故障注入→重试恢复）、concurrency 7、web 32、m6 31、m6_web 20、m7_web 42、m7 89 —— 末行 `REGRESSION OVERALL: PASS` 即全绿。单脚本可独立运行（各 `verify_*.py` / `e2e/verify_*.mjs`）。

交付前最后一次全链结果：**10/10 PASS**（2026-08-20）。验收证据与逐项复跑命令见 `deploy/ACCEPTANCE.md`。

## 七、交付物清单

| 文件 | 内容 |
|---|---|
| 架构设计.md | 唯一权威设计（含 §十一 部署/验收/图片嵌入等全部决策，只追加） |
| deploy/ACCEPTANCE.md | M3–M7 各里程碑验收记录 + 复跑命令 |
| 功能点.md | 需求原文（第二部分为本项目） |
| 交付文档.md | 本文件 |
| docs/img_embed_check/ | PDF 模板原版 vs 生成版逐页对比图 |
| docs/sample_pages/ | 原版模板逐页参考图 |
| 学生成长报告册.pdf | 原版纸质模板（复刻基准） |
| report-server/scripts/verify_*.py + run_regression.sh | 服务端永久验证脚本 |
| report-web/e2e/verify_*.mjs | 前端 E2E（Chrome） |

## 八、已知事项与注意事项

1. **§14/§15 为范围裁剪**，非缺失；对外说明写「范围外」即可。
2. **§7 九格画像雷达图在 PDF 报告内**呈现（四线对比 + 优劣势维度），前端无独立画像页。
3. `verify_m7_web` 偶发时序抖动（历史上出现过一次 41/42），复跑即绿，脚本本身无问题。
4. 渲染器改动部署：`cd report-renderer && mvn -s settings.xml package -DskipTests`，无需重启后端；**不要 `mvn clean` 后不起服务就期望能渲染**（渲染 classpath 在 target/ 下）。
5. MinIO 中旧报告会随重新生成累积（每次生成插入新记录）；当前库内 52 名学生的报告均为 2026-08-20 新模板产物，历史旧件已清理。
6. Docker Desktop 偶发整体崩溃（三容器同灭）时后端报 500 —— 先 `docker ps` 看容器，等 healthy 即自愈。
