# 项目说明 · 石实实验学校「数智成长」中学素质报告平台（面试向）

> 本文档是项目的**总说明**，面向两类读者：① 想快速了解本项目全貌的人；② 作者本人面试准备。
> 写法遵循「解决什么问题 → 用什么方案 → 为什么这么选 → 核心怎么实现 → 踩过什么坑 → 怎么验证」。
> 运行/部署的操作细节见 README.md，接口明细见 docs/接口文档.md，需求原文见 功能点.md。

---

## 一、项目解决什么问题

### 1.1 业务痛点（真实场景）

初中素质教育评价有三件麻烦事：

1. **学期《学生成长报告册》靠手工做**。一份 50+ 页的图文报告（封面、理念、九维雷达图、每科一页学业分析、成长银行账本、班主任寄语……），过去用 Word/PS 拼版，班主任做一个班要几天，且每年重做。
2. **过程性数据散落各处**。成绩在 Excel、课堂表现在班主任笔记本、活动照片在手机相册、荣誉证书在抽屉——期末想汇总时数据凑不齐。
3. **素质评价难量化、难持续**。「德智体美劳」喊了很多年，但缺乏一套天天在记录、学期末能出结论的量化体系。

### 1.2 解决方案（一句话）

**把「采集数据 → 形成成长档案 → AI 分析 → 教师干预 → 一键生成成长报告」整条闭环搬到线上**：教师日常随手录成绩/评价/活动/荣誉/微光瞬间，系统自动聚合九维素质画像，AI 起草寄语与总结（教师确认生效），期末一键渲染 **1:1 复刻纸质模板的《学生成长报告册》PDF**（单生/全班/全年级批量）。

### 1.3 量化成果

| 指标 | 数值 |
|---|---|
| 报告复刻度 | 51 页 PDF 逐页与纸质模板对齐（含微光掠影的学生 52 页），聚合数据与设计基线（golden）**零漂移** |
| 生成速度 | 单份空闲约 16s（30s 内交付）；50 人班级批量 189s；全年级 52 份 185s 零失败 |
| 质量门禁 | 服务端回归 10 脚本约 160 断言 + 前端 E2E 94 断言（桌面 1440 / 手机 390 双档）全绿；RBAC 权限用例 18/18 |
| AI 体系 | 寄语/总结/证书识别三类能力，未配 key 自动降级规则模板，功能永不可用性为零 |
| 交付形态 | `docker compose up -d --build` 五容器一键起全栈，空库自动建表+种子 |
| 开发周期 | 约 2 周完成设计→Web 全功能交付（2026-08-17 确认设计 → 08-22 容器化交付），随后 1 周完成换校/九维改名/移动端教师工作台（App 化第一步） |

---

## 二、总体架构

### 2.1 架构图

```text
┌────────────────────────────────────────────────────────────────┐
│  浏览器（响应式，一套代码两种形态）                                │
│  桌面：Element Plus（报告工作台 + 系统管理）                       │
│  移动：Vant 4 底部 Tabbar（首页/班级/微光随手拍/成长流/通知/我的）   │
└──────────────┬─────────────────────────────────────────────────┘
               │ REST/JSON，JWT Bearer（无状态）
┌──────────────▼─────────────────────────────────────────────────┐
│  report-server：Spring Boot 3 单体（模块化包结构）                 │
│  认证/RBAC数据域 │ 成绩/评价引擎 │ 活动荣誉 │ 微光信箱 │ 教师档案    │
│  AI服务层（规则事实 + LLM + 任务队列 + 降级） │ 报告任务编排         │
│  运维：Flyway迁移/审计Filter/登录防爆破/定时清理                    │
└───┬──────────────┬──────────────────┬──────────────────────────┘
    │              │ fork 子进程        │
┌───▼───┐   ┌──────▼───────────────┐  ┌─▼──────────┐  ┌─────────┐
│ MySQL │   │ report-renderer      │  │ Redis      │  │ MinIO   │
│ 业务库 │   │ (独立Maven模块,冻结)  │  │ 防爆破计数  │  │ PDF/照片 │
└───────┘   │ Thymeleaf+ECharts    │  │ 任务进度缓存│  └─────────┘
            │ +Playwright 打印PDF   │  └────────────┘
            └──────────┬───────────┘
                 ┌─────▼─────┐
                 │ LLM API   │ OpenAI 兼容协议（DeepSeek/通义/智谱均可换）
                 └───────────┘
```

### 2.2 三个代码模块（同仓库）

| 模块 | 技术 | 职责 |
|---|---|---|
| `report-web` | Vue 3 + TS + Vite；桌面 Element Plus / 移动 Vant 4 | 全部 UI。桌面管重表格（成绩、管理端），移动管随手记录（微光拍照、班级动态） |
| `report-server` | Spring Boot 3.3.5 / Java 21 / MyBatis-Plus | 全部业务：认证、RBAC、评价引擎、AI 服务层、报告任务编排、审计运维 |
| `report-renderer` | Thymeleaf + ECharts 5.6(svg) + Playwright(Chromium) | **冻结的渲染核心**：输入报告数据 JSON，输出 51 页 PDF。无 Spring 依赖，可独立 main 运行 |

**为什么渲染器是独立模块、由 server fork 子进程执行，而不是 server 内直接调用？**

1. **崩溃隔离**：Chromium 是原生进程，段错误/内存泄漏不能拖垮 Web 服务；超时直接 `destroyForcibly()` 杀掉即可。
2. **免重启换版**：renderer 只需 `mvn package` 更新 target/，server 每次 fork 都读最新 classpath——改模板不发版、不重启（演示/迭代期巨大便利）。
3. **并发可控**：渲染是 CPU+内存大户（单次 -Xmx512m），用独立线程池 + 优先级队列调度，与 Web 线程池彻底解耦。

### 2.3 两条核心链路

**① 期末「一键生成报告」链路**：

```text
教师点击生成(单生/班/年级)
 → t_report_task 建任务(总数/进度/失败数) → 入渲染池(优先级:单份0 批量1)
 → ReportDataBuilder 聚合该生全部数据 → 报告 JSON（与设计基线 golden 同构）
 → fork JVM: RenderPdf(JSON) → Thymeleaf 组 HTML → 页内 ECharts 画图
    → waitForFunction(chartsReady) → page.pdf() A4 margin0 → 落盘
 → PDF 上传 MinIO → 任务状态回写 → 前端轮询进度 → 预览/下载
```

**② 日常「一次过程性评价」链路（写穿设计）**：

```text
任课教师选学生→选指标→±分
 → 同步写穿 6 个目标：
   ① t_evaluation 明细
   ② t_grid_stat_term 学期九维累计（upsert）
   ③ t_grid_stat_week 周九维（upsert）
   ④ t_coin_week 周能量币（只动本人）
   ⑤ 能量币流水+账户（CoinLedgerService 唯一入账口）
   ⑥ 班级/年级九维均值增量平移（+= s/人数）
 → 九维雷达图、周折线、成长银行、报告对应版块全部自动更新
```

报告**只读预聚合表、Java 零聚合代码**——这是「读写路径分离」的取舍：读路径（报告）永远快而稳定，写路径（评价）承担一致性维护成本。

---

## 三、技术选型与理由（面试高频）

| 层 | 选型 | 为什么选它 / 为什么没选别的 |
|---|---|---|
| **PDF 生成** | Thymeleaf 模板 + 页内 ECharts + Playwright 无头打印 | 决定性约束：报告图表密集（雷达/柱状/周折线），ECharts 需要 JS 执行环境。备选：① WeasyPrint/openhtmltopdf——CSS 支持有限，52 页复杂绝对定位版式风险高，且 ECharts 还得另做 Node SSR（双运行时）；② iText 手工排版——复刻纸质模板等于重写排版引擎。浏览器打印 = 用「世界上兼容性最好的排版引擎」渲染 HTML/CSS，保真度天花板，模板即前端技术栈，前后端同学都能改 |
| 前端 | Vue 3 + TypeScript | 教育信息化开源生态（可参考项目）几乎全是 Vue+SpringBoot，二次开发成本最低；TS 保证双 UI 形态共享类型 |
| 桌面/移动双 UI | Element Plus（桌面）+ Vant 4（移动）同一工程 | 桌面管理端重表格重表单，移动端是教师「随手拍/随手记」场景，交互范式完全不同；同仓同路由同 JWT，避免两套工程的鉴权/构建/部署翻倍 |
| 后端 | Spring Boot 3 + MyBatis-Plus | 单校规模（<100 教师并发）用单体 + 模块化包结构足够；MP 省去 90% 样板 SQL，复杂口径（评价写穿）仍手写 LambdaQuery 保证精确可控 |
| 数据库 | MySQL 8 | 关系型业务主数据（评价、成绩、账本），聚合表按（学生,学期,维度,周）建联合语义 |
| 缓存 | Redis | 只存「丢了能重建」的东西：登录失败计数（防爆破）、任务进度展示缓存。**刻意不让业务正确性依赖 Redis** |
| 对象存储 | MinIO | PDF 报告、证书、微光照片、教师/学生照片统一对象名空间（`teacher/{id}/{uuid}.jpg` 等），S3 协议可平替云 OSS |
| AI 接入 | 自封装 OpenAI 兼容 HTTP 客户端（非 LangChain4j） | 只用 chat/completions + vision 两个能力，引入框架反而增加依赖面；协议兼容 = DeepSeek/通义/智谱/Kimi 换一行配置即切换 |
| 任务队列 | DB 事实表 + ThreadPoolExecutor（自研轻量） | 任务量级是「学期末几千份报告/每天几百条 AI」，不需要 Kafka/RabbitMQ 的吞吐；**DB 表即队列**天然带持久化、重启恢复、进度可查 |
| 部署 | Docker Compose 五容器 | 单机单校，K8s 是为多实例弹性设计的，这里没有多实例诉求——复杂度最小化 |
| Excel | Apache POI 5.3 | EasyExcel 3.x 绑定 POI 4.1.2/JDK11 且社区停维护，直用 POI 无版本冲突 |
| CI | GitHub Actions | 推送/PR 到 main：后端 mvn package + 前端 npm build，守住「可构建」底线 |

**架构层面最大的两个「不做」**：不拆微服务（规模不匹配，运维成本先于收益到来）；不做实时推送（轮询 2s 在任务面板场景完全够，SSE/WebSocket 的连接管理复杂度不值得）。

---

## 四、核心模块与关键实现

### 4.1 PDF 1:1 复刻渲染流水线（项目技术含金量最高的部分）

**三层契约锁定「零漂移」**——这是整个渲染体系的质量基石：

| 层 | 锁什么 | 怎么验 |
|---|---|---|
| 数据层 | 聚合 JSON 与设计基线 `golden_student.json` **深度全等** | `verify_contract.py` 深比较（数值按 JS 等价） |
| 模板层 | 渲染 PDF 页数与**每页标题序列**与基线 PDF 逐页一致 | PyMuPDF 提取两份 PDF 标题序列比对 |
| 契约保护 | 「学生1」是金样本：任何写入测试后必须「除年级均值外零差异」 | 测试自带 mysqldump 快照信封，跑毕恢复+复验 |

意义：模板/聚合代码的任何改动，只要契约不过，CI/回归立刻红——**51 页复杂版式敢持续迭代的底气来自这里**（后续加封面页、加微光掠影版块、整校换名都安全落地）。

**server 侧：渲染进程池（优先级插队）**——`RenderService.java` 核心：

```java
// 池：固定并发 + 优先级队列 —— 单份(0)永远先于批量(1)
pool = new ThreadPoolExecutor(concurrency, concurrency, 0, TimeUnit.MILLISECONDS,
        new PriorityBlockingQueue<>());

private Path render(String batchKey, Long studentId, Long termId) throws Exception {
    Map<String, Object> data = dataBuilder.build(studentId, termId);   // 聚合
    objectMapper.writeValue(json.toFile(), data);                      // 落盘 JSON
    Process process = new ProcessBuilder(javaExecutable(),
                    "-Dfile.encoding=UTF-8", "-Xmx512m", "-cp", rendererClasspath(),
                    "com.aischool.render.RenderPdf",
                    json.toAbsolutePath().toString(), pdf.toAbsolutePath().toString())
            .directory(new java.io.File(rendererHome)).redirectErrorStream(true).start();
    // 子进程 stdout 落 render.log；超时强杀；exit!=0 抛业务异常带日志尾部
    if (!process.waitFor(timeoutSeconds, TimeUnit.SECONDS)) {
        process.destroyForcibly();
        throw new BizException(500, "渲染超时（>" + timeoutSeconds + "s）");
    }
    ...
}
```

效果：批量满载时教师插队生成单份仍 24s 内出稿（并发验收实测）。

**renderer 侧：Thymeleaf → ECharts → Playwright**——`RenderPdf.java` 核心：

```java
Context ctx = new Context();
ctx.setVariable("r", data);
ctx.setVariable("dataJson", om.writeValueAsString(data));
ctx.setVariable("echartsJs", resourceText("/static/echarts.min.js")); // ★ 内联
ctx.setVariable("imgLogo", dataUri("/static/img/img_logo.png"));      // ★ 图片转 base64 data URI
String html = engine.process("report", ctx);

try (Playwright pw = Playwright.create()) {
    Browser browser = pw.chromium().launch(
            new BrowserType.LaunchOptions().setArgs(List.of("--no-sandbox"))); // 容器 root 必需
    Page page = browser.newPage();
    page.navigate(htmlOut.toAbsolutePath().toUri().toString());      // file:// 打开
    page.waitForFunction("window.chartsReady === true");             // ★ 等全部图表画完
    page.pdf(new Page.PdfOptions().setPath(outPdf).setFormat("A4")
            .setPrintBackground(true)
            .setMargin(new Margin().setTop("0").setBottom("0")      // margin 全 0
                    .setLeft("0").setRight("0")));
}
```

三个关键细节（每个都是踩坑换来的）：

1. **JS/图片全部 base64 内联进 HTML**：`file://` 协议下相对路径资源加载不可靠，echarts.min.js、12 张模板图全部以变量注入——HTML 自包含，跨机器/容器行为一致。
2. **`waitForFunction(chartsReady)`**：模板里所有 ECharts 渲染完在 finally 置 `window.chartsReady=true`，比 sleep 等待又快又稳；失败时 evaluate 诊断信息（echarts 是否加载、chart 错误）写日志。
3. **margin=0 + 页眉页码模板内绝对定位**：浏览器原生页眉页脚不可控，改为模板每页 CSS 定位渲染——这也让「页码/封面/页眉 logo」这类版式问题全部收敛在模板层解决。

**版式复刻方法论**：PDF pt 与 CSS pt **1:1**，模板按原版 bbox 绝对定位复刻；原版图片用 PyMuPDF 提取，**smask 软蒙版必须手工合并为 alpha**（直接 extract_image 会丢蒙版），照片类压 JPEG、真透明保 PNG，6–10 倍过采样降到 ~300dpi 视觉无损。

### 4.2 过程性评价写穿链（读写路径分离的一致性设计）

`EvaluationService.evaluate()` 一次评价按固定顺序写穿 6 个目标（明细→学期格→周格→周能量币→流水/账户→班/年级均值）。三个口径对齐要点：

```java
// ① 学期窗口 [start 00:00, end 00:00] 闭区间 —— 与 ReportDataBuilder 读取过滤逐字一致
// ② kindCount = (title + 指标名) 去重组数 —— 与报告记录卡分组同构
private int weekNo(Term term, LocalDateTime evalTime) {
    long days = ChronoUnit.DAYS.between(term.getStartDate().atStartOfDay(), evalTime);
    return (int) (days / 7) + 1;                       // ③ 周号口径与种子一致
}
// ④ 班/年级均值增量平移：avg += s/人数（scale 4 HALF_UP），避免全量重算
```

**无 @Transactional 是有意取舍**：单校低并发场景下，6 个写目标的部分失败残差由后续重录的 upsert 语义自然收敛；引入事务反而把长事务带进聚合表写路径。验证方式是**快照信封**：写入类测试跑前 mysqldump、跑毕恢复、再跑全量契约兜底「零污染」。

### 4.3 AI 防幻觉三层设计

寄语/总结写给家长看，**数字错一个都是事故**。三层防御：

1. **规则引擎算硬数字**：`RuleFactsService.facts()` 从 DB 聚合出「总分/班级最高/九维积分/活动荣誉/微光时刻」等结构化事实，**LLM 拿到的是算好的数**，不自己算。
2. **提示词约束 + 输出清洗**：

```java
private static final String SYSTEM_TEACHER = "你是一位经验丰富、温和严谨的中学班主任。"
    + "写作时只允许使用用户提供的数据，严禁编造或修改任何数字与事实；"
    + "语气真诚具体，避免空话套话；不出现「根据数据显示」这类机械表述；"
    + "输出纯文本，禁止使用任何 Markdown 标记。";
// stripMd() 兜底清洗 ** 残留；parseBlocks() 兼容「标题独行/标题同行」两种输出
```

3. **AI 只产草稿，教师确认制**：LLM 结果落 `t_comment.ai_draft`（状态「AI草稿」），教师改后保存才「已确认」进报告；`已确认` 的内容永不被新草稿覆盖。

**降级链路**：未配 key / 调用失败 / 超时（读超时 120s，实测 LLM 可达 61s）→ 自动降级**确定性规则模板**（`templateComment()`，数字照样准确），任务仍算成功（source=template），前端有提示。**AI 永远是增强，不是依赖**。

### 4.4 AI 任务队列（t_ai_task + worker 池）

需求：切页/关浏览器不中断、整班批量、多教师互不干扰、并发贴合供应商限额。

```java
@PostConstruct
public void init() {
    pool = new ThreadPoolExecutor(concurrency, concurrency, 60, TimeUnit.SECONDS,
            new LinkedBlockingQueue<>(10_000), r -> { ... });
    // ★ 重启恢复：DB 里「排队/生成中」的任务全部重置回排队重新入队（内存队列不可信，DB 才是事实）
    for (AiTask t : taskMapper.selectList(...in(status, "排队", "生成中"))) {
        if ("生成中".equals(t.getStatus())) { t.setStatus("排队"); taskMapper.updateById(t); }
        pool.execute(() -> run(t.getId()));
    }
}

public Long submit(String type, Long studentId, Long termId, Long userId) {
    AiTask exist = taskMapper.selectOne(...未完成同任务...);  // ★ 去重复用：省 API 账单
    if (exist != null) return exist.getId();
    ...插入 t(排队) → pool.execute(() -> run(id)) → return id;
}
```

设计要点：

- **并发默认 16**：按 DeepSeek 官方限额（flash 2500/账号）取 0.6%，超限只 429 不封号；实测吞吐 ≈1 任务/秒（17 任务 15s）。**所有教师共用一个池**——并发不随教师数放大，天然全局限流。
- **隔离**：`mine` 按 created_by 过滤，任务详情走学生数据权限校验。
- **结果持久化分层**：寄语落 `t_comment`（永久）；总结只落任务 result_json（90 天清理，文档明示副作用）。
- token 用量随任务落库 → 管理端「AI 用量」按日/按教师聚合。

### 4.5 RBAC 数据域隔离

三角色：ADMIN（全校）/ HEAD_TEACHER（所带班）/ TEACHER（任课班）。核心是 `DataScopeService` 两个方法：`visibleClassIds()`（列表过滤）与 `checkStudentAccess()/checkClassOperable()`（单点校验），**所有业务入口必须过其一**，越权统一 403。18 个权限用例（无 token/坏 token/跨班读写/任课越权）回归覆盖。

写权限三档有意差异（需求驱动）：日常评价=数据可见即可评（任课教师日常打分）；活动/荣誉/综评/寄语=班主任；成绩录入=该科任课教师（学科负责制）。

### 4.6 微光信箱 → AI 上下文 → PDF 成长掠影（App 化阶段新增的特色闭环）

**场景**：教师手机随手拍下学生闪光瞬间（课间互助、劳动、比赛），选场景标签+备注+圈选学生。这些**照片级过程素材**流向三处：

1. **班级动态轮播 / 学生「闪光时刻」**（移动端班级页、学生详情页）。
2. **AI 上下文**：`AiDraftService.injectMomentFacts()` 把该生本学期最近 6 条微光（日期/场景/教师备注）注入 facts——**LLM 与降级模板两条路都可见**，寄语从「只看分数」变成「有画面感」：

```java
private void injectMomentFacts(Map<String, Object> facts, Long studentId, Long termId) {
    ... // 该生关联的微光，按学期区间过滤，LIMIT 6
    row.put("日期", ...); row.put("场景", m.getSceneTag()); row.put("教师备注", m.getNote());
    facts.put("微光时刻（教师随手拍下的闪光记录）", rows);
}
```

3. **PDF「成长掠影」版块**：server 端从 MinIO 读原图 → 缩到最长边 720px JPEG（**垫白底防透明偏色**、BILINEAR）→ base64 data URI 内嵌 JSON → 模板 `th:if` 非空才渲染。**无微光的学生页数不变**（51 页契约不动），有微光的学生多一页掠影（52 页）。

照片加载统一走 **JWT fetch-blob 模式**：`<img>` 标签带不了 Authorization 头 → 前端 fetch 拉流 → `URL.createObjectURL()` —— 这是全项目图片端点（微光/证书/封面/档案照）的通用解法。

### 4.7 教师档案（含一个典型 ORM 坑）

`t_teacher_profile` 主键=账号 id（无自增）。**MyBatis-Plus 的坑**：`@TableId` 不写 type 默认 ASSIGN_ID（雪花号），会**覆盖手工 set 的主键值**——现象是「首存 200 但行消失」：雪花 id 插不进、后续又以「主键非 null」误判走 updateById（0 行无异常，静默丢数据）。修复两件套：

```java
@TableId(type = IdType.INPUT)   // ① 主键由调用方传入
private Long userId;

// ② 插入/更新分支按 DB 存在性判断，而非「主键是否为 null」
boolean exists = profileMapper.selectById(userId) != null;
if (exists) profileMapper.updateById(p); else profileMapper.insert(p);
```

工号唯一 = 应用层查重（提示占用者姓名）+ DB `UNIQUE KEY uk_employee_no` 双保险；照片换图删旧 MinIO 对象防孤儿。

### 4.8 移动端教师工作台（App 化第一步）

同仓新增 Vant 4 移动形态（AppTabbar 底部导航）：首页（今日概览）/ 班级（名单+动态轮播）/ 微光随手拍（相机或相册，多选学生+场景标签）/ 成长记录流 / 通知 / 我的（含教师档案编辑页：Vant popup-picker 表单，照片圆钮点击换图）。桌面 EP 与移动 Vant **共享路由、JWT、Pinia**，为后续 Capacitor 打壳做准备（壳内 Camera API 已按 `Capacitor.isNativePlatform()` 分支预留）。

---

## 五、踩过的坑与修复（面试故事素材）

| # | 坑 | 根因 | 修法 |
|---|---|---|---|
| 1 | **PDF 下载随机截断** | StreamingResponseBody 异步派发再次穿过 Security 链，JWT 过滤器（OncePerRequestFilter）跳过 ASYNC dispatch → 上下文空 → 连接被掐 | 改 byte[] 同步返回（PDF 约数百 KB~3MB，可接受） |
| 2 | **教师档案首存静默丢数据** | MP 默认 ASSIGN_ID 雪花号覆盖手工主键 + 「主键非 null」误判走 update | `IdType.INPUT` + 按 selectById 存在性分支（见 4.7） |
| 3 | **LLM 慢响应击穿** | 实测 LLM 可达 61s，读超时 60s 把慢任务误判失败 | 超时 120s；超时降级模板而非任务失败 |
| 4 | **LLM 输出带 Markdown** | `**加粗**`/`# 标题` 混进寄语 | 提示词禁止 + stripMd 兜底 + parseBlocks 跨行收集（兼容标题独行） |
| 5 | **E2E 偶发 30s 超时** | 路由离场过渡 160ms 期间旧页面仍在 DOM，「waitForURL 后立即点 nth(1)」命中旧页面的 select | **离场过渡必须零时长**（`.page-fade-leave-active{transition:none}`）——任何非零离场都会给「共享选择器+立即点击」类测试留竞态窗口 |
| 6 | **Redis 空转异常** | Lettuce command timeout(5s) 与 BLPOP 阻塞(5s) 同值赛跑 | timeout 提到 20s |
| 7 | **分页 total=0** | 未注册 MyBatis-Plus 分页拦截器 | `PaginationInnerInterceptor` 配置类 |
| 8 | **容器构建卡字体包** | apt 走 archive.ubuntu.com 拉 61MB 字体超时 | apt/npm/maven 全切国内镜像源（sed 兼容 deb822 与旧 sources.list） |
| 9 | **docker exec 写中文乱码** | mysql 客户端默认字符集 | `--default-character-set=utf8mb4` |
| 10 | **同刻评价顺序漂移** | 3796 条评价大量同 eval_time，`ORDER BY eval_time` 平局顺序依赖优化器计划（DB restore 后 DDL 变更曾致契约失败） | 追加 `ORDER BY id` 决胜序（对「聚合器零改动」红线的最小偏离） |

**方法论级的经验**（讲出来加分）：

- **视觉 QA 误报甄别**：视觉模型说「同一照片重复出现」「卡片高度不齐」时，用数据反驳——base64 长度两两不同证非同图；嵌入栅格宽高比全 ≈0.78 恰为卡片比 140/180、且六卡中心像素取色均为照片色而非卡底色，证 object-fit:cover 生效。**先量化，再下结论**。
- **契约先行**：52 页 PDF 这种「改一处、崩一片」的产物，先立 golden 基线再动刀，之后每次模板迭代都有安全网。
- **演示数据与契约数据分班**：初一(1)班=契约基线勿动，初一(2)班=随便玩——演示与验证永不打架。

---

## 六、质量保障体系

| 层 | 手段 |
|---|---|
| 数据契约 | `verify_contract.py`：聚合 JSON ≡ golden 深比较 + PDF 页数/标题序列逐页比对（退出码与结果一致，可作门禁） |
| 服务端回归 | 10 脚本约 160 断言：contract / rbac 18 / ai 11 / retry 4（故障注入→重试恢复）/ concurrency 7 / m6 31 / m7 89；`run_regression.sh` 一键全链 |
| 前端 E2E | 94 断言，Chrome 三视口（390/768/1440），覆盖批量生成→进度→预览→下载全流程 + 无横向滚动检查 |
| 写入零污染 | 写入类脚本自带 mysqldump 快照信封：跑毕恢复 + 契约复验，金样本「除年级均值外零差异」 |
| CI | GitHub Actions：推送/PR 到 main 跑后端 mvn package + 前端 npm build |
| 容器自检 | `d3_check.py`（nginx:80 端到端）/ `d3_render_check.py`（容器内渲染 PDF 中文字体无方块） |

---

## 七、部署与运维

### 7.1 形态与升级路径

- **一键起栈**：`docker compose up -d --build` → mysql/redis/minio/report-server/report-web 五容器，空库首启 Flyway 自动建表+种子。
- **升级**：后端 `mvn package` → build/up report-server（新迁移 Flyway 自动执行）；前端 `npm run build` → build/up report-web；**渲染模板只需 renderer `mvn package`，无需重启任何服务**。
- AI 配置走 gitignored 的 `docker-compose.override.yml`（base-url/api-key/model 三项，留空=降级模板）。

### 7.2 运维基线

Flyway 迁移（只追加禁改历史）｜全部写操作审计留痕（180 天）｜登录防爆破（Redis 失败计数→429 锁定）｜五容器日志轮转 50MB×3｜`backup.sh`（mysqldump+MinIO→backups/，建议 cron）｜每日 03:17 定时清理（AI 任务 90 天，保留期可配）。

### 7.3 公网上线检查单（要点）

改 JWT 密钥｜改 MySQL/Redis/MinIO 三件默认密码｜关 Swagger（`SPRINGDOC_API_DOCS_ENABLED=false`）｜CORS 白名单改正式域名｜仅暴露 80、数据端口限内网｜AI key 只放 gitignored 文件｜备份挂 cron｜演示账号改密或停用。

### 7.4 容量与扩容思路（被问「学生多了怎么办」）

读路径已预聚合（报告只读聚合表），瓶颈只在渲染吞吐：并发池可配（4~8/实例）、全年级批量天然可夜间错峰、失败可重试断点续跑；再往上加机器即横向扩 server 实例（渲染 fork 是无状态的，只需共享 MinIO/DB）。AI 侧并发独立可调，按供应商限额设。

---

## 八、面试问答要点

**Q1：为什么用无头浏览器打印 PDF，不用 iText/WeasyPrint？**
图表密集（ECharts 雷达/折线/柱状）+ 51 页复杂绝对定位版式。浏览器是 CSS 兼容性最好的排版引擎，保真度天花板；模板=HTML+CSS，前端技术栈直接复用。备选方案要么 CSS 支持有限（openhtmltopdf），要么 ECharts 还得 Node SSR 双运行时（WeasyPrint 路线），要么等于重写排版引擎（iText）。代价是渲染重（进程级），用 fork 子进程池+优先级队列消化。

**Q2：51 页 PDF 怎么保证改版不回归？**
契约测试三层锁：聚合 JSON 与 golden 深比较全等；渲染 PDF 页数+每页标题序列与基线逐页一致；金样本学生写入测试用快照信封保证零污染。任何一层漂移，回归立刻红。

**Q3：渲染为什么 fork 子进程？**
崩溃隔离（Chromium 原生进程不能拖垮 Web 服务，超时可强杀）；免重启换版（fork 每次读最新 classpath，改模板不发版）；资源隔离（单次 -Xmx512m，与 Web 线程池解耦）。

**Q4：批量生成怎么调度？**
DB 任务表 + ThreadPoolExecutor + PriorityBlockingQueue：单份优先级 0、批量 1，批量满载时单份插队仍 24s 出稿；50 人 189s、全年级 52 份 185s 零失败；失败可重试（故障注入验证过：破坏 classpath→failed→还原→retry→全成功）。

**Q5：LLM 幻觉怎么防？**
三层：规则引擎算好全部硬数字再注入提示词（模型只组织语言不计算）；system prompt 严令「只用提供的数据」+输出清洗；AI 只产草稿、教师确认制，已确认内容不被覆盖。另加降级：未配 key/失败/超时→确定性模板，数字照样准确。

**Q6：AI 任务为什么自研队列不用 MQ？**
量级是学期末几百条/天，DB 表即队列：天然持久化、重启恢复（@PostConstruct 重置入队）、进度可查、去重省账单。MQ 的吞吐优势在这个量级换不来复杂度收益。并发 16 按 DeepSeek 限额的 0.6% 取值，全体教师共用一个池=全局限流。

**Q7：数据权限怎么做的？**
RBAC 三角色 + DataScopeService：visibleClassIds 决定列表过滤，checkStudentAccess/checkClassOperable 决定单点校验，所有业务入口必过其一；18 个越权用例回归覆盖。写权限按业务语义分三档（评价=可见即可评、班级事务=班主任、成绩=学科任课）。

**Q8：评价写入没加事务，一致性怎么保证？**
取舍：单校低并发，长事务进聚合表写路径得不偿失；写路径 upsert 语义让部分失败残差可自然收敛；用快照信封+契约复验兜底验证。如果并发上量，升级方案是把 6 写目标收进一个 @Transactional 或改事件驱动最终一致。

**Q9：为什么单体不微服务？**
单校 <100 教师并发、单机部署。微服务的收益（独立扩缩、团队边界）在这个规模都不成立，成本（分布式事务、链路追踪、运维面）先来。用「模块化包结构 + 渲染/AI 两类重负载独立进程池」拿到隔离收益的大部分。

**Q10：前后端怎么分工图片这种带鉴权的二进制？**
JWT 放 header，`<img>` 带不了 → 统一 fetch-blob→objectURL 模式；PDF 预览下载用 byte[] 同步返回（曾用 StreamingResponseBody 因 ASYNC dispatch 穿 Security 链导致随机截断，改为同步返回根治）。

**Q11：印象最深的 bug？**
教师档案首存静默丢数据：表象是 200 成功但行消失。双根因：MP 默认 ASSIGN_ID 雪花号覆盖手工主键；分支判断用「主键是否为 null」而主键总被 set，恒走 updateById（0 行无异常）。修法 IdType.INPUT + 按 DB 存在性分支。教训：ORM 的「默认策略」要在建表第一天弄清，静默失败要靠「写后回读」验证发现。

**Q12：项目最大的技术亮点？**
一条龙：以「契约零漂移」为纲的 51 页 PDF 复刻流水线（fork 进程池+优先级+故障恢复），加上「规则算数、LLM 组词、人工确认」的防幻觉 AI 层，和「读写路径分离」的评价聚合引擎——三件事互相咬合：评价写穿养聚合表，聚合表喂报告与 AI，报告靠契约敢迭代。

---

## 九、演进路线（已完成 → 进行中）

- **已完成**：Web 全功能（M1–M7）→ 一键容器化 → AI 接入+任务队列 → 运维扩容批次 → 整校换装（东华→石实，含种子/模板/前端资产单一事实源传导）→ 九维更名「五育·扬长」→ 移动端教师工作台（Vant）+ 微光信箱全链路 + 教师档案。
- **进行中**：Capacitor 打壳成 App（相机/推送原生能力）、公网服务器部署（域名+备案，按 7.3 检查单）、签名打包分发——**完整操作手册见 §十**。
- **换校 SOP**（平台产品化的关键一步已验证）：改 golden 生成器 school 块 → 重跑 expand_golden/seed_db → 替换 4 张静态图 → 前端资产与文案 → 刷新契约基线。半天级完成一校换装。

---

## 十、上线实操手册（部署 → 上线 → App 打包分发全流程）

> 2026-08 起实操路径的完整记录，供日后复习：从「代码在仓库」到「服务器跑起来 + 老师手机装上 App」。
> 核心认知：**App 只是个壳，真正的东西全在服务器——先服务器，后 App。**

### 10.0 全景

```text
【0 准备资产】→【A 服务器部署】→【B 上线安全+AI】→【C App 打包分发】→【D 日常运维】
 买服务器/域名      体检→拉码→起容器       改密码/关文档        签名/出APK/装手机     更新/备份
 （花钱、备案慢）    （半天）               （1小时）            （半天）              （每次10分钟）
```

### 10.1 资产准备（花钱和做决定的都在这）

| 东西 | 要不要 | 要点 | 费用 |
|---|---|---|---|
| 云服务器 | 必须 | Ubuntu 22.04；**4G 内存起步**（后端镜像含 Chromium 渲染 PDF，2G 会卡死）；地域就近 | 2核4G ≈ ¥200-400/年 |
| 域名 | 强烈建议 | 没有也能跑（App 填 `http://IP:端口`），但微信打不开、无 HTTPS、不正式 | ¥30-60/年 |
| 备案 | 国内服务器+域名必须 | 工信部登记，**1~3 周，全计划最慢环节，最先启动**；等待期用 IP+高位端口先跑 | 免费 |
| HTTPS 证书 | 建议最终上 | Let's Encrypt / 云厂商免费送，一条命令 | 免费 |
| AI key | 可选 | 不接自动降级模板 | 充 ¥10 够试很久 |

### 10.2 服务器部署

**① 体检（部署前必做，共用服务器尤其要查撞车）**：

```bash
uname -a && head -2 /etc/os-release     # 系统版本（Ubuntu 22.04 为佳）
free -h && df -h / && nproc             # 内存/磁盘/核数
docker --version && docker compose version
ss -tln | grep -E ':(80|8080|3306|6379|9000) '   # 端口占用（80 常被现有 nginx 占）
docker ps --format 'table {{.Names}}\t{{.Ports}}' # 已有容器
```

**② 权限（共用服务器的常见挡点）**：普通账号不在 `docker` 组 → 一条 Docker 命令都执行不了。两条路：请管理员 `usermod -aG docker`（本服务器未获批）；或 **Docker Rootless**（本服务器实际采用）——守护进程跑在自己用户态，无需任何授权。接入只要两步：

```bash
docker context create rootless --docker host=unix:///run/user/<uid>/docker.sock
docker context use rootless     # 此后 docker/compose 命令全部作用于自己的守护进程
```

Rootless 是**独立守护进程、独立配置目录 `~/.config/docker/`**：国内服务器 Docker Hub 被墙，系统级 `/etc/docker/daemon.json` 的加速源不影响它，要自己抄一份 registry-mirrors 进去再 `systemctl --user restart docker`（配 Linger 后断开 SSH 容器照常 7×24 运行）。

**③ 远程操作通道**：FinalShell（给人用的 GUI 终端）和 paramiko（Python SSH 库，给程序用）走同一 SSH 协议、同一端口，发出的命令一模一样。凭据管理：**只放本机 gitignored 的 `local/server.json`**（配 `local/sshrun.py` 执行器，sudo 密码走 stdin 不进命令行），绝不入仓库、不贴聊天。`local/` 已在 .gitignore。

**④ 部署**：

```bash
git clone https://github.com/wow-wogua/AI_school.git    # 从 GitHub 拉，与仓库逐字一致
cd AI_school && docker compose up -d --build            # 本机一键起栈：五容器，首次国内源 20-60 分钟
# 服务器（长构建放后台跑，断线不杀）：加 server 覆盖文件收敛端口，日志落盘；
# 若服务器上建了 gitignored 的 docker-compose.override.yml（CORS/AI key），再追加第三个 -f
nohup docker compose -f docker-compose.yml -f docker-compose.server.yml up -d --build \
  > ~/aischool-build.log 2>&1 &
```

**⑤ 端口冲突处理**：共用服务器 80 等常用端口不许占 → 仓库自带 `docker-compose.server.yml` 覆盖文件：用 `!override []` 清空 mysql/redis/minio/server 的端口映射（容器间走 compose 内部网络互访），仅 web 发布高位端口 `18086:80`（访问 `http://<服务器IP>:18086`，安全组需放行）。`!override` 是 Compose v2.24+ 标签——普通合并只会追加条目、删不掉基础文件的端口，收敛端口必须用它。验收在浏览器/App 进行；本机才用 `d3_check.py` 直连 8080 自检。

### 10.3 上线安全检查 + AI 接入（全是改配置，1 小时）

**先建立「公网认知」，这张单才有意义**：部署到公网 = 任何知道 IP:端口的人都能打开登录页——这是所有网站的常态（微信、淘宝同理）。三个认知：

1. **IP 藏不住，也不是安全边界**。域名只是好记、不藏 IP（`ping` 一下就出来）；全网扫描器（Shodan 这类）7×24 扫整个地址段，高位端口只是**降噪**（避开最常见的 80/8080 扫描），不是防护。
2. **安全靠「扫到了也打不进」**。关卡从外到内：登录+JWT（没 token 一律拒）→ 防爆破限流（反复试密码被锁）→ RBAC 数据域（登录了也只能看自己班）；且端口收敛后**公网上只有 nginx 一扇门**——3306/6379/9000/8080 没映射出来，连被扫的机会都没有。
3. **真风险是具体的**：弱口令、默认 JWT 密钥、明文 HTTP——正是下面七条存在的原因。另外：仓库是 public 的，**服务器 IP 不写进任何提交文件**（文档用 `<服务器IP>` 占位，真实 IP 只在本机 gitignored 的 `local/`）——这层只是降低被定向关注的概率，不是防线。

| # | 检查项 | 做法 |
|---|---|---|
| 1 | JWT 密钥 | 改掉默认 `aischool.jwt.secret`（否则可伪造 token） |
| 2 | 三件默认密码 | MySQL root / MinIO 的 aischool123 全改（compose 环境变量） |
| 3 | 关 Swagger | `SPRINGDOC_API_DOCS_ENABLED=false`（否则接口全暴露） |
| 4 | CORS 白名单 | 属性 `aischool.cors.allowed-origins`（默认只放行 dev 的 localhost:5173）；服务器上经 gitignored 的 override 传 `AISCHOOL_CORS_ALLOWED_ORIGINS`（公网地址、App 的 `https://localhost`），将来换域名同步改 |
| 5 | 端口收敛 | 只对公网开 80/443；3306/6379/9000/8080 限内网 |
| 6 | 演示账号 | README 上写着 admin/admin123——上线前全部改密或停用 |
| 7 | AI 接入 | 服务器上建 gitignored 的 `docker-compose.override.yml` 填 base-url/api-key/model 三项，重启 report-server 即接入（代码零改动）；`backup.sh` 挂 cron |

**延伸：威胁模型思维（面试好用）**——服务分「怕被找到」和「怕被攻破」两类：代理节点类服务的死法是 **IP 被封锁**，所以那类教程说「买个域名更安全」，实际指的是域名签 TLS 证书把流量伪装成普通 HTTPS、再藏到 CDN 后面抗封锁；而本项目是正规网站，IP 公开无所谓，敌人是**入侵者**，防线是认证、权限、改掉默认凭据。域名+HTTPS 对本项目的意义是**加密传输+备案合规+好记**，不是藏 IP。同一句「域名更安全」，在两种威胁模型下说的是完全不同的两件事——先问「我的敌人是谁」，再谈安全手段。


### 10.4 App 打包、签名、分发

**⓪ 先想明白：App 与服务器的关系（一个端口服务两种形态）**

```
电脑浏览器 ── http://<服务器IP>:18086 ──┐
                                        ├→ nginx 容器（唯一对外的门）
手机 App ──── http://<服务器IP>:18086 ──┘
```

- **不需要为 App 单开端口**：浏览器用户和 App 用同一个 18086。区别只在谁提供界面——浏览器来的，nginx 发页面+API；**APK 里已封进全部界面文件**，不找服务器要页面，运行时只发 HTTP 请求要数据。所以是「瘦客户端」：手机只存 token+服务器地址，服务器停了 App 能打开但登录不了、一条数据都没有
- **服务器地址两层来源**（`http.ts` 的 `apiBase()`，优先级从高到低）：登录页/「我的」页手填（存 localStorage `serverBase`）→ 打包时注入 `VITE_API_BASE` 默认值。开发期用手填（IP/端口变了不用重打包），交付版注入默认（老师装上即用）
- **换域名不用发新 APK**：备案后绑域名+HTTPS，用户在登录页把地址改成 `https://域名` 即可——当初做成可配置而非写死，就是为了这天
- **明文 HTTP 的安卓坑（两道闸，都要开）**：① 网络层——Android 默认禁止 App 发明文请求，需要清单文件 `usesCleartextTraffic="true"`；② 内容策略层——Capacitor 界面默认跑在 `https://localhost`，「https 页面发 http 请求」属**混合内容**，WebView 默认直接拦截，需要 `capacitor.config.ts` 里 `android: { allowMixedContent: true }`。**只开 ① 的症状极具迷惑性：App 界面正常、点登录毫无反应也无报错、服务器日志一条请求都没有**（请求在手机里就被掐了，根本没出门）——真机首跑实测踩过。配套修法：登录 catch 网络层失败弹 toast，绝不让失败静默。将来服务器上了 HTTPS（备案+域名）这两处都可关回更严谨
- **Capacitor 配置静默吞错键**：`allowMixedContent` 在 TS 类型声明（declarations.d.ts）里**没有**，但运行时（Capacitor Android 的 `CapConfig.java`）认它——写错键名（比如凭记忆写 `mixedContentMode: 'allow'`）会原样进 JSON 然后被 Java 端无视，构建不报任何错。**排查法：直接 grep `node_modules/@capacitor/android/.../CapConfig.java` 看运行时到底读哪个键**，别信记忆也别全信类型声明


**① 签名密钥（一次性，最重要的一步）**：

```bash
keytool -genkey -v -keystore aischool.keystore -alias aischool ^
  -keyalg RSA -keysize 2048 -validity 36500
```

- 这是 App 的「公章」；debug APK 用的是公共调试章，**正式发布必须有自己的章**
- **keystore + 密码丢了 = 这个 App 永远无法发更新**：备份两处（U盘/网盘）、密码自己管、永不入仓库
- 有效期 36500 天，一劳永逸

**② 配置签名 + 打包**：`android/app/build.gradle` 加 signingConfig（密码读 gitignored 的 `keystore.properties`，仓库只留占位模板）；`versionCode` 递增；然后：

```bash
cd report-web && npm run build && npx cap sync android
cd android && gradlew assembleRelease
# 产物 android/app/build/outputs/apk/release/app-release.apk
```

**③ 分发（按正式程度三选一）**：直接发 APK 文件（微信群/网盘，老师装时允许「未知来源」，校内工具**推荐**）｜蒲公英/fir.im 扫码分发（体面+统计）｜应用市场（要企业开发者账号 ¥300/年 + 软著 1-2 月，**不划算**）。

**④ 更新**：改 versionCode → 重新打包 → 发新 APK 覆盖安装；登录态与数据都在服务器，不丢。真机验收链：装 App → 填服务器地址 → 登录 → 拍微光 → 生成报告 → 下载 PDF。

### 10.5 日常运维

| 场景 | 操作 |
|---|---|
| 改代码更新 | 服务器 `git pull` → `docker compose build xxx && up -d xxx`（迁移 Flyway 自动跑） |
| App 发版 | versionCode+1 → 重新打包发新 APK |
| 备份 | `deploy/backup.sh` 挂 cron 每日（MySQL+MinIO） |
| 看日志 | `docker logs 容器名`（轮转 50MB×3 已配） |
| 磁盘 | 主要吃盘是 MinIO 里累积的 PDF/照片，定期清理 |

### 10.6 时间线（按关键路径排）

```text
第 1 天    买服务器+域名，当天提交备案（最慢环节先启动）；部署跑通（IP 直连先用，App 也能连）
第 1-2 天  安全检查单 + AI 接入 + 签名打包 APK 发到手机
1~3 周后   备案下来 → 绑域名 + HTTPS → App 里地址换成域名
```

### 10.7 实操进度（2026-08-30）

- ✅ 服务器体检：Ubuntu 22.04 / 112 核 62G / 磁盘充足——**共用服务器**，已跑着他人多个服务（Neo4j、Ollama、nginx 占 80 等）
- ✅ 权限落定：docker 组未获批 → 管理员指路 **Docker Rootless**（subuid/Linger 已配好）→ `docker context` 已切 rootless（Server 29.5.2，凭据见本机 `local/`）
- ✅ rootless 镜像加速：`~/.config/docker/daemon.json` 抄系统四源 → 重启守护进程 → 试拉 redis 镜像验证通过
- ✅ 代码就位：服务器浅克隆仓库（GitHub 直连可达），已 pull 到含 server 覆盖文件的提交
- ✅ 构建两跑：首跑遇**单层卡死**（8KB/s 瘫 20 分钟；杀掉重踢即恢复——已完成层有缓存不亏。教训：**进程活着≠在动，要盯日志增长**）；二跑过夜完成，五容器自动起栈全 healthy
- ✅ 外网验收（8-30 晨）：公网首页 200（22ms）、登录 API 发真 JWT、移动端五页全真实数据、**console 零报错零失败请求**
- ✅ 深链路验证：**容器内 PDF 生成全通**——API 发起→任务队列→fork 渲染进程→Chromium→MinIO→带 JWT 下载，8 秒出报告（3.6MB/51 页，Noto CJK 字体正常嵌入无方块）
- ✅ 踩坑修复 **CORS 403**：curl 直接 POST 能通、浏览器登录被拒——差异在 **Origin 头**（curl 不带→绕过校验；浏览器带→白名单里只有 dev 的 5173 被拒）。修法零代码：服务器 gitignored 的 `docker-compose.override.yml` 传 `AISCHOOL_CORS_ALLOWED_ORIGINS`（公网地址+App 的 `https://localhost`），重建 server 容器 25 秒恢复。**诊断口诀：curl 通浏览器不通，先想 CORS/Origin**
- ✅ 安全加固（8-30）：JWT 换 96 位随机密钥；MySQL root / MinIO 密码更换（`ALTER` 与 override 环境变量、healthcheck 三处联动改）；Swagger 双保险关闭（**实测 nginx 本就不转发 `/v3`——返回的是 SPA 兜底 html，容器内开关再关一层**）；**17 个账号全量改密**（admin/litao 独立随机密码，15 个教师演示账号共用一个；旧 admin123 实测 401 拒绝）
- ✅ 加固后全链路复验：PDF 生成 12 秒/3.6MB 正常（新数据库密码+新 MinIO 密钥+新 JWT 全在业务里验证）；**双形态 26 路由清扫零报错**（litao 移动 6 路由 + admin 桌面 20 路由含系统管理 8 页签/学生详情/PDF 预览）；容器零崩溃重启、日志无真实错误
- ✅ 备份上线：`backup.sh` 服务器实跑通过（MySQL 224K + MinIO 3.8M 双产物），cron 每日 03:17 自动备份 + 14 天轮转；凭据档案 `~/aischool-creds.txt`（chmod 600）
- ✅ APK 签名打包（8-30）：keytool 生成密钥库（RSA 2048/50 年，gitignored）→ `build.gradle` 注入 signingConfig（密码读 gitignored 的 `keystore.properties`，存在才启用——fork 者无此文件也能构建）→ `VITE_API_BASE` 注入公网地址构建前端（开箱即连，无需手填）→ `cap sync` → `gradlew assembleRelease`；**apksigner 验证 V2 签名通过**；签名密码记本机 `local/creds.md`。版本轨迹（真机联调一路修）：v1.0 初包 → v1.0.1 失败 toast+错键 → v1.0.2 `allowMixedContent` 真键（网络通）→ v1.0.3 报错按 HTTP 码分流 → **v1.0.4 班级页遮挡修复（当前交付版）**，产物 `local/release/shishi-growth-v1.0.4.apk`
- ✅ 真机首跑翻车+两轮修复（8-30）：v1.0 装机后点登录**无反应无报错**——服务器 nginx 日志 40 分钟零请求（先用 curl 探针验证日志管道是通的，才敢下"请求没出门"的结论）→ 定位为混合内容拦截（见 10.4 ⓪）+ 登录无 catch 静默吞错。**第一轮修复翻车**：凭记忆写 `mixedContentMode: 'allow'`——不存在此键被静默忽略，v1.0.1 依然连不上（但新增的失败 toast 生效了，不再静默）；手机浏览器同网实测能开站+4 次 401（旧密码），证明手机网络路径没问题、卡点必在 WebView。**第二轮**：grep Capacitor 运行时源码找到真键 `android.allowMixedContent`，v1.0.2 修正。**教训三条：①先验证观测手段本身再读观测结果；②配置不生效先怀疑键名——去运行时源码里 grep 它读什么；③手机浏览器同网测试是分辨"网络不通 vs App 内部问题"的利器**
- ✅ 第三轮"假网络错误"（8-30）：v1.0.2 请求已到服务器（nginx 日志 UA 带 `wv` 即 WebView），但仍弹「连接服务器失败」——真相是 **401 密码错 + 429 防爆破锁定**被 `http.ts` 的"401=会话过期"分支一锅端、又被登录页 catch 误报成网络错。修复（v1.0.3）：`httpError` 携带 HTTP 状态码，登录页按码分流——401→「用户名或密码错误」、429→「尝试过于频繁」。**教训：登录接口的 401 与会话过期同码不同义，登录页必须自己解释；日志里 UA 带不带 `wv` 是分辨 App/浏览器流量的标志**。防爆破策略备查：同用户名 15 分钟 5 败 → 锁 10 分钟（按用户名非 IP，无共享桶问题）
- ✅ 班级页遮挡修复（8-30，v1.0.4）：空态微光卡被学生列表上叠 40px 盖住下半截——历史残留的条件 `overlap`（误以为空态时卡片不存在）。修法：列表永不上叠。**防复发**：QA 扫描脚本升级为「卡片两两矩形相交检测」，18 路由全量扫过 0 重叠 0 报错；顺带排查全部 6 处 `overlap` 用法（NoticeView 的条件 overlap 是真·首卡消失补偿，非同病）
- ⬜ 待办：（可选）AI key 接入 → 全部完成后清理本地（服务器 build cache 7.8G 可清）并建议改服务器密码

---

## 附：仓库地图

```text
report-web/          Vue3 前端（桌面 EP + 移动 Vant 双形态）
  src/views/         21 视图（Reports/Score/Evaluate/…/Home/Class/MomentCapture/Mine/Profile…）
  src/components/    AppTabbar(移动导航)/MomentPhoto/admin/*Tab(管理端 8 页签)
  e2e/               verify_web/m6_web/m7_web（94 断言）
  scripts/           qa_*.mjs（Playwright 质检脚本组）
report-server/       Spring Boot 后端
  controller/        REST 入口（Auth/Score/Evaluation/Honor/Activity/Ai/Report/Moment/Profile/Admin*）
  service/           eval(写穿引擎)/ai(规则事实+草稿+任务队列)/report(聚合+渲染池+任务)/coin(账本)…
  db/migration/      Flyway V1–V4
  scripts/           verify_*.py 回归组 + run_regression.sh + d3_*.py 容器自检
report-renderer/     冻结渲染核心（templates/report.html 51 页模板 + static/img 模板图）
  scripts/           expand_golden.py（golden 生成器=换校单一事实源）
deploy/              schema.sql/seed.sql/backup.sh
Dockerfile / docker-compose.yml / nginx.conf / .github/workflows/ci.yml
README.md（运行手册）· docs/接口文档.md（API 全量）· 功能点.md（需求原文）· 本文档
```
