# 教师学生数智化成长平台 · 架构设计

> 版本：v0.2（关键假设已确认）
> 确认记录（2026-08-17）：① 技术栈 Spring Boot 3 + Vue 3；② PDF 生成采用 Playwright 无头浏览器打印 HTML 模板；③ 家长端置于 M6 后期 —— 三项均已确认采用
> 范围：仅覆盖《功能点.md》**第二部分「教师学生数智化成长平台」**（第一部分安卓数字人问答系统不在本设计范围内）
> 最终交付形态：Web 平台 + **一键生成《学生成长报告册.pdf》**（对标本目录样例，52 页图文报告）

---

## 一、项目概述

### 1.1 核心业务闭环

```text
采集数据 → 形成成长档案 → AI 分析 → 教师干预 → 生成成长报告(PDF)
```

### 1.2 目标报告拆解（样例 PDF 逆向分析）

对 `学生成长报告册.pdf`（52 页）逐页解析后，报告由以下**可模板化的版块**组成：

| # | 版块 | 页码 | 内容 | 数据来源 |
|---|------|------|------|----------|
| 1 | 封面 + 目录 | 1 | 学校名、学期、学生信息 | 学生档案 + 学期 |
| 2 | 学校理念 | 2 | 教育理念、校训等 | 报告模板配置（全校统一） |
| 3 | 学校风采 | 3 | 图片 + 简介 | 模板配置 |
| 4 | "九格"育人体系 | 4 | 九格说明图文 | 模板配置 |
| 5 | 校长寄语 | 5 | 图片/文字 | 模板配置 |
| 6 | 学业综合得分发展 | 7 | 各科成绩横向柱状图、个人总分 vs 班级/年级最高分、过程性评价总分横向/周纵向对比 | 成绩表 + 评价记录聚合 |
| 7 | 各科学业评价 | 8-18 | **每科一页**：学科维度图、期末成绩/学业评价对比、学科过程性评价（正向/负向指标 + 周数据折线） | 成绩表 + 评价记录 |
| 8 | 我的平时成绩 | 19-20 | 平时成绩明细表 | 成绩表 |
| 9 | 个人综合素质成长 | 22 | **九维雷达图**（本学期 vs 上学期）、优势/待努力维度、成长箴言 | 九格评价聚合 |
| 10 | 九格逐格发展 | 23-42 | **每格一节**：发展概述（积分/次数/种类）、横向分析（个人/班级/年级）、纵向分析（周折线）、评价记录明细卡片 | 评价记录 |
| 11 | 我的活动 | 43-44 | 参与的活动列表（空数据显示鼓励语） | 活动表 |
| 12 | 成长银行 | 45-48 | 能量币（现有/已支出/可兑换 + 汇率）、横向分析、周收入、收入/支出 TOP 模块、支出明细 | 积分账户 |
| 13 | 成长象征物 | 49-50 | 当前积分、等级 Lv、距下一级、象征物图片 | 积分账户 |
| 14 | 综合素质评定 + 班主任寄语 | 51 | 五维 A/B/C/D 等级 + 寄语正文 | 综合素质评定 + AI 寄语 |
| 15 | 学生自评 | 52 | 上学期回顾、新学期展望 | 学生自评 |
| 16 | 家长寄语 | 53 | 家长填写 | 家长寄语 |

**关键结论**：报告 = 固定模板版块（1-5）+ 数据驱动循环版块（6-14，每个学科/每格/每个学生重复生成）+ 采集型版块（15-16）。图表密集（雷达图、柱状图、周折线、明细卡），是技术选型的关键约束。

---

## 二、GitHub / 开源生态调研结论

### 2.1 可直接二次开发的项目

| 项目 | 地址 | 技术栈 | 契合度 | 结论 |
|------|------|--------|--------|------|
| StudentManageSystem | [ruanjiancheng/StudentManageSystem](https://github.com/ruanjiancheng/StudentManageSystem) | Vue + Spring Boot + MyBatis，管理员/教师/学生三角色 | ★★★☆ | 成绩管理、角色权限可直接借鉴；**无九格评价、无 PDF 报告、无 AI** |
| 学生综合素质评价系统 | [腾讯云开发者文章](https://cloud.tencent.com/developer/article/2306929)（Vue+SpringBoot，学生/教师/教务处三端，多维度评价） | Vue + Spring Boot | ★★★ | 多维评价模型最接近，可参考其评价维度设计 |
| Student | [gitee.com/zeng987/student](https://gitee.com/zeng987/student.git) | SpringBoot + Vue | ★★ | 成绩管理参考 |
| RuoYi-Vue | [gitee.com/y_project/RuoYi-Vue](https://gitee.com/y_project/RuoYi-Vue) | Spring Boot + Vue + RBAC + 代码生成 | ★★★ | 作为**脚手架**最稳：权限、字典、Excel 导入导出、定时任务开箱即用 |
| OnlineEdu | [github.com/snwjas/OnlineEdu](https://github.com/snwjas/OnlineEdu) | SpringBoot + Vue | ★★ | 教务架构参考 |

### 2.2 调研结论

1. **没有一个开源项目同时覆盖「九格评价 + 能量币成长银行 + AI 分析 + 52 页 PDF 成长报告」**。这些恰是本项目的核心特色（功能点.md §6 明确指出九格是"整个系统的核心特色"），必须自研。
2. 通用底座（用户/角色/权限、班级树、成绩 CRUD、Excel 导入）有大量成熟轮子 → **不重复造轮子，基于 RuoYi-Vue 或 StudentManageSystem 起步**。
3. PDF 报告生成选型（见 §3.3）：ECharts 图表需要 JS 执行环境，主流两条路线——**无头浏览器打印**（Puppeteer/Playwright，保真度高，见[方案对比](https://developer.cloud.tencent.com/article/2556037?policyId=1003)、[调研文章](https://juejin.cn/post/7338259035043561498)）或 **ECharts SSR 出 SVG + WeasyPrint**（轻量，见 [ECharts 官方 SSR 文档](https://echarts.apache.org/handbook/zh/how-to/cross-platform/server/)、[WeasyPrint 实践](https://textdata.cn/blog/weasyprint_auto_report/)）。本项目图表与前端完全同源，选**无头浏览器路线**。

> 详细调研来源见文末「参考资料」。

---

## 三、总体架构

### 3.1 架构图

```text
┌─────────────────────────────────────────────────────────────────┐
│                        用户层（浏览器）                           │
│   学校管理员 │ 班主任 │ 任课教师 │ 学生(自评) │ 家长(后期)        │
└──────────────┬──────────────────────────────────────────────────┘
               │ HTTPS
┌──────────────▼──────────────────────────────────────────────────┐
│              前端 SPA：Vue 3 + Element Plus + ECharts            │
│  管理端 │ 班主任工作台 │ 任课教师工作台 │ 学生/家长端(后期)        │
└──────────────┬──────────────────────────────────────────────────┘
               │ REST / JSON
┌──────────────▼──────────────────────────────────────────────────┐
│                  后端单体：Spring Boot 3（模块化）                │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────────────┐    │
│  │ 用户权限  │ │ 基础信息  │ │ 成绩管理  │ │ 九格评价引擎      │    │
│  │ 模块      │ │ 模块      │ │ 模块      │ │ (指标/记录/聚合)  │    │
│  └──────────┘ └──────────┘ └──────────┘ └──────────────────┘    │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────────────┐    │
│  │ 活动荣誉  │ │ 成长银行  │ │ 档案时间轴│ │ 报告生成模块      │    │
│  │ 模块      │ │ 模块      │ │ 模块      │ │ (模板+任务+PDF)   │    │
│  └──────────┘ └──────────┘ └──────────┘ └──────────────────┘    │
│  ┌──────────────────────┐  ┌───────────────────────────────┐    │
│  │ AI 服务层            │  │ 定时聚合任务                    │    │
│  │ (学业分析/寄语/总结)  │  │ (周数据/班级年级均值/能量币)     │    │
│  └──────────────────────┘  └───────────────────────────────┘    │
└───────┬──────────────┬──────────────┬──────────────────────────┘
        │              │              │
   ┌────▼────┐   ┌─────▼─────┐  ┌─────▼─────────────────────┐
   │ MySQL 8 │   │  Redis    │  │ 报告渲染器（独立进程）      │
   │ 业务数据 │   │ 会话/缓存 │  │ Playwright-Java + ECharts │
   └─────────┘   └───────────┘  │ HTML模板 → PDF             │
        │                       └─────┬─────────────────────┘
   ┌────▼──────────┐                │ PDF 归档
   │ MinIO / 本地盘 │◀───────────────┘
   │ 照片/证书/PDF  │
   └───────────────┘
        ▲
   ┌────┴───────────────┐
   │ LLM API            │  OpenAI 兼容接口（与数字人项目一期环境一致）
   │ 可选本地 OCR/视觉模型│  证书照片识别
   └────────────────────┘
```

### 3.2 技术选型及理由

| 层 | 选型 | 理由 |
|----|------|------|
| 前端 | Vue 3 + TypeScript + Element Plus + Pinia + ECharts | 与可参考开源项目同栈；ECharts 是样例报告图表的天然对齐物 |
| 后端 | Spring Boot 3 + MyBatis-Plus + Spring Security(JWT) + RBAC | 教育信息化主流栈；可直接借鉴 §2.1 项目；EasyExcel 导入成熟 |
| 数据库 | MySQL 8 | 关系型业务数据（评价记录、成绩）为主 |
| 缓存 | Redis | 会话、班级/年级均值缓存、生成任务进度 |
| 文件 | MinIO（或本地磁盘 + Nginx） | 学生照片、活动照片、证书、生成的 PDF 归档 |
| **PDF 生成** | **Thymeleaf 模板 + ECharts（页面内渲染）+ Playwright-Java 打印** | 一套 ECharts 配置同时服务"网页看图"和"PDF 出图"；52 页复杂版式（每科一页、每格一节循环 + 分页控制）用 CSS `@page` 处理；保真度最高 |
| AI | Spring 调用 OpenAI 兼容 HTTP 接口（LangChain4j 可选） | 功能点.md 第一部分已注明"技术验证环境已接入 OpenAI 兼容大模型接口"，保持一致，可随时切换供应商 |
| Excel | EasyExcel | 成绩批量导入（功能点 §3） |
| 定时任务 | Spring Scheduler（后期量大再上 XXL-Job） | 周数据聚合、均值计算、批量报告生成 |

**备选方案（说明权衡，主方案已于 2026-08-17 确认）**：
- *全 Python（FastAPI + Jinja2 + WeasyPrint + ECharts SSR）*：AI 生态更顺、更轻量；但可参考的开源教育项目几乎全是 Java/Vue，团队协作与二次开发成本更高。若团队是 Python 背景可切换此方案，架构不变。
- *纯 Java 无浏览器方案（ECharts SSR + openhtmltopdf）*：部署最轻，但 ECharts 需 Node/JS 环境做 SSR，引入双运行时；openhtmltopdf 对 CSS 支持有限，52 页复杂版式风险高。不推荐首选。

### 3.3 报告生成流水线（核心技术设计）

```text
① 老师选择「2026年春季学期」→ 点击生成（单个 / 全班 / 全年级）
        │
② 后端创建异步任务(task) → 入队
        │
③ 聚合层：按学生并行抽取
      成绩(个人/班级最高/年级最高) + 评价记录(周聚合) + 九格积分
      + 活动 + 能量币账本 + 综合素质评定 + 寄语/自评 → 报告数据 JSON
        │
④ 渲染层（Playwright 池，4~8 并发）
      Thymeleaf 组装 HTML（16 个版块模板，学科页/九格页循环生成）
      页内 ECharts 渲染（雷达图/柱状图/周折线）→ 等待渲染完成
      page.pdf()：A4、页眉页脚（"东莞市XX初级中学学生成长报告册"+页码）
        │
⑤ PDF 上传 MinIO → 任务状态回写（进度经 Redis → 前端轮询/SSE）
        │
⑥ 下载 / 打印 / 归档；学期结束批量跑：错峰 + 断点续跑 + 失败重试
```

要点：
- **模板与数据彻底分离**：版块 1-5（学校理念/风采/九格体系/校长寄语）由管理员在「报告模板管理」里配置，换学校只改配置；
- **空数据兜底**：样例第 43 页"我的活动"为空时显示鼓励语——每个循环版块都要定义空态；
- **批量性能预估**：50 人班级 × 每份 15~30s ≈ 4 并发下 5 分钟内完成，全年级（~1000 人）约 1.5~2 小时，安排在夜间定时执行。

---

## 四、数据模型（核心表）

> 命名前缀 `t_`，以下列为主干字段，实施时补充审计字段（create_time / update_time / deleted）。

```text
t_user                用户(id, username, password_hash, real_name, role, phone, status)
t_grade               年级(id, name, school_year)
t_class               班级(id, grade_id, name, head_teacher_id→t_user)
t_student             学生(id, user_id?, student_no, name, gender, class_id,
                            enroll_date, status, photo_url,
                            guardian_name, guardian_phone)
t_term                学期(id, name(2026春季), start_date, end_date, is_current)
t_subject             学科(id, name, type(国家课程/校本), sort)

t_exam                考试(id, term_id, name(期中/期末/月考), exam_date)
t_exam_subject        考试科目(id, exam_id, subject_id, full_score)
t_score               成绩(id, exam_id, subject_id, student_id, score,
                            class_rank, grade_rank, created_by)
t_regular_score       平时成绩(id, student_id, subject_id, term_id, score, date)

── 九格评价引擎 ──
t_grid                九格(id, code(GG/XG/XG2/PG/ZG/HG/MG/TG/TG2), name(国格…), icon, sort)
t_indicator           二级指标(id, grid_id, name(课堂表现/作业表现…),
                            direction(正/负), default_score, subject_scope)
t_evaluation          评价记录(id, student_id, teacher_id, indicator_id,
                            score, remark, eval_time)
                      ← 一次评价经 indicator.grid_id 自动归入对应格

── 聚合表（定时任务生成，报告与画像直接读这里）──
t_grid_stat_week      九格周聚合(student_id, term_id, grid_id, week_no, score)
t_grid_stat_term      九格学期聚合(student_id, term_id, grid_id, score, eval_count, kind_count)
t_class_grid_avg      班级九格均值(class_id, term_id, grid_id, avg_score)
t_grade_grid_avg      年级九格均值(grade_id, term_id, grid_id, avg_score)

── 活动与荣誉 ──
t_activity            活动(id, title, type, start_time, place, cover_url, intro, creator_id)
t_activity_signup     参与记录(id, activity_id, student_id, signup_time,
                            checkin_time, award, performance, eval_text)
t_honor               荣誉证书(id, student_id, name, level, issuer, honor_date,
                            file_url, ai_parsed(jsonb), confirm_status)
                            ← AI 识别结果先落 ai_parsed，教师确认后生效

── 成长银行 / 象征物 ──
t_coin_account        能量币账户(student_id, current_coin, total_coin)
t_coin_rate           汇率配置(id, rate, effective_date)
t_coin_income         能量币收入(id, student_id, source_type(评价/活动/荣誉),
                            source_id, score, coin, create_time)
t_coin_expense        能量币支出(id, student_id, item, coin, create_time)
t_growth_level        成长象征物等级(id, level(Lv.1..), min_score, symbol_name, symbol_img)
                      ← 积分自动映射等级（样例：1020分 → Lv.3 → 下一级还需172分）

── 综合素质 / 寄语 ──
t_comprehensive       综合素质评定(id, student_id, term_id,
                            moral(A/B/C/D), ability, practice, aesthetic, health,
                            final_level)
t_comment             寄语(id, student_id, term_id, type(班主任/学生自评/家长),
                            content, ai_draft, status(AI草稿/已修改/已确认))

── 报告 ──
t_report_template     报告模板(id, school_name, logo_url, sections(jsonb 各版块配置), status)
t_report_task         生成任务(id, term_id, scope(单生/班/年级), target_id,
                            status(排队/进行中/成功/失败), total, done, failed, create_by)
t_report              成品报告(id, task_id, student_id, file_url, page_count,
                            gen_time, status)
```

### 数据流示例（对应功能点 §5-§7）

```text
任课教师「课堂积极发言 +2」
   → t_evaluation(indicator=智格-课堂表现, score=+2)
   → 定时聚合 → t_grid_stat_week / t_grid_stat_term(智格)
   → 能量币：score×汇率 → t_coin_income → t_coin_account
   → 九格雷达图 / 周折线 / 时间轴 / 报告第 29-33 页（智格发展）全部自动更新
```

---

## 五、模块设计 ↔ 功能点映射

| 功能点 | 平台模块 | 说明 |
|--------|----------|------|
| §1 用户与权限 | 用户权限模块 | RBAC 三角色（管理员/班主任/任课教师），数据权限按班级/学科过滤；学生、家长账号后期开 |
| §2 学生档案 | 基础信息 + 档案聚合页 | 基本信息 CRUD + 成长信息只读聚合展示（初一到初三轨迹） |
| §3 成绩管理 | 成绩管理模块 | 单条录入 + EasyExcel 批量导入 + 排名自动计算（班级/年级） |
| §4 学业成长分析 | AI 服务层·学业分析 | 规则引擎(趋势/离群检测)先算硬指标，LLM 生成自然语言解读；输出：优势学科/待提升/变化趋势/异常提醒 |
| §5 平时表现评价 | 九格评价引擎 | 选学生→选指标→±分值→备注，一条记录同时进九格与成长银行 |
| §6 九格综合素质评价 | 九格评价引擎 | 指标体系管理员可配置（每格下二级指标增删改） |
| §7 九格画像 | 档案聚合页 | 九维雷达图：本学期/上学期/班级平均/年级平均 四系列；自动输出优势/待努力维度 |
| §8 事件时间轴 | 档案聚合页 | 评价/活动/荣誉/成绩进步统一事件流，按时间排序 |
| §9 活动管理 | 活动荣誉模块 | 活动 CRUD + 报名/签到/获奖/表现，自动入档 |
| §10 荣誉与证书 | 活动荣誉模块 | 上传照片/PDF → AI 视觉模型识别(奖项/级别/时间/主办单位) → 教师确认 |
| §11 综合素质评定 | 综合素质模块 | 五维 A/B/C/D 录入 + 自动算综合结果 |
| §12 班主任寄语 | AI 服务层·寄语生成 | 读取成绩变化/九格/活动/荣誉/日常评价 → 生成草稿 → 老师改 → 保存 |
| §13 AI 成长总结 | AI 服务层·成长总结 | 生成四块：亮点/学习发展/综合素质发展/下一阶段建议；Prompt 约束"不只看成绩" |
| §14 学生自评 | 自评模块（学生端） | 引导式问卷（三问）→ AI 整理成文，学生确认提交 |
| §15 家长寄语 | 家长端（后期） | 查看报告/趋势、写寄语、签字确认 |
| §16 一键生成报告 | 报告生成模块 | 见 §3.3 流水线；单个/全班/全年级批量 |

---

## 六、AI 服务层设计

统一 `AiService`，三种调用场景共用「数据组装 → Prompt 模板 → OpenAI 兼容接口 → 结果落库（草稿态）」流程：

| 能力 | 输入 | 输出 | 落库 |
|------|------|------|------|
| 学业分析 | 成绩序列、排名、离群点 | 结构化结论(优势/待提升/趋势/异常) | t_report 附带 + 前端展示 |
| 班主任寄语 | 成绩变化、九格积分、活动、荣誉、近期评价 | 150~300 字寄语草稿 | t_comment.ai_draft |
| 成长总结 | 同上 + 综合素质评定 | 四块总结 | t_report 附带 |
| 证书识别 | 证书图片 | 奖项名/级别/时间/主办单位 JSON | t_honor.ai_parsed |
| 学生自评辅助 | 引导式三问的回答 | 整理后的自评文本 | t_comment(学生自评) |

原则：
1. **AI 只产草稿，教师/学生确认后生效**（功能点 §12：生成 → 修改 → 保存）；
2. 硬数字（排名、分差、均值）**全部由规则引擎计算**，LLM 只负责把它们组织成自然语言，避免幻觉数字；
3. Prompt 与模型参数存配置表，便于调优与换模型。

---

## 七、API 概览（REST）

```text
/api/auth/**                 登录、刷新、修改密码
/api/admin/**                教师/年级/班级/学生/学期/学科/指标体系/报告模板管理
/api/score/**                成绩录入、Excel 导入、排名查询、趋势/雷达数据
/api/evaluation/**           评价记录（选学生→选指标→打分），指标列表
/api/student/**              学生档案、九格画像、时间轴（数据权限过滤）
/api/activity/**             活动 CRUD、报名签到、获奖
/api/honor/**                荣誉上传、AI 识别、确认
/api/comprehensive/**        五维评定
/api/ai/**                   寄语草稿、成长总结、自评辅助（均异步或流式）
/api/report/**               模板配置、生成任务(单个/班/年级)、进度、下载、归档
```

---

## 八、部署架构

```text
[nginx] ── 静态(Vue dist) + 反向代理 /api
   │
[Spring Boot 主服务]        [报告渲染器(同仓库独立模块/进程)]
   │        │                     │ Playwright-Java(内置 Chromium)
 MySQL    Redis                │ 临时文件
   │                             ▼
 MinIO/本地盘  ◀──────────────  PDF 归档
   │
 [LLM API]  OpenAI 兼容接口（内网代理/公网）
```

- 开发/演示：单机 docker-compose（mysql + redis + minio + 后端 + 渲染器）。
- 校内生产：一台应用服务器即可（预计并发 < 100 教师同时操作），批量报告夜间跑。
- Playwright-Java 需在部署环境安装 Chromium 依赖（Linux 需 `playwright install --with-deps chromium`）。

---

## 九、开发路线（建议分期）

| 期 | 内容 | 验证标准 |
|----|------|----------|
| M1 基座 | 脚手架(参考 RuoYi/StudentManageSystem) + RBAC + 年级/班级/学生/学期管理 | 三角色登录，数据权限正确 |
| M2 数据采集 | 成绩管理(Excel 导入+排名) + 九格指标配置 + 日常评价记录 | 一次评价 → 九格聚合 + 能量币入账正确 |
| M3 聚合与画像 | 周聚合任务 + 班级/年级均值 + 九格雷达图 + 时间轴 | 样例学生数据 → 雷达图与样例一致 |
| M4 AI 能力 | 学业分析 / 班主任寄语 / 成长总结 / 证书识别 | 寄语草稿可生成→修改→保存；数字无幻觉 |
| M5 报告 | 16 版块模板 + 渲染流水线 + 批量任务 | 生成的 PDF 与样例结构一致（逐版块比对），全班批量成功 |
| M6 活动/荣誉/自评/家长端 | 活动管理、荣誉上传识别、学生自评、家长寄语 | 空/满数据版块均正常渲染 |

> M5 的「逐版块比对样例 PDF」作为核心验收标准，建议先手工造一个"黄金学生"数据集，全链路对齐后再铺开。

---

## 十、关键风险与对策

| 风险 | 对策 |
|------|------|
| 52 页 PDF 版式还原度 | 版块模板化 + CSS `@page` 分页控制；先做 M5 黄金样本比对再批量 |
| 批量生成耗时长/失败重试 | 任务表 + 断点续跑 + 夜间错峰 + 并发 Playwright 池 |
| LLM 数字幻觉 | 硬指标由规则引擎计算，LLM 只做语言组织；输出带数据引用 |
| 评价数据量大后聚合慢 | 周聚合/均值走定时任务落聚合表，报告只读聚合表 |
| 与数字人项目（一期）集成 | 共用学校组织架构数据；保持 OpenAI 兼容接口同源，后期可统一账号 |

---

## 参考资料（调研来源）

- [StudentManageSystem（GitHub，Vue+SpringBoot 成绩管理）](https://github.com/ruanjiancheng/StudentManageSystem)
- [Vue+SpringBoot 学生综合素质评价系统（腾讯云开发者）](https://cloud.tencent.com/developer/article/2306929)
- [zeng987/Student（Gitee）](https://gitee.com/zeng987/student.git)
- [snwjas/OnlineEdu（GitHub）](https://github.com/snwjas/OnlineEdu)
- [RuoYi-Vue 脚手架（Gitee）](https://gitee.com/y_project/RuoYi-Vue)
- [ECharts 官方服务端渲染文档](https://echarts.apache.org/handbook/zh/how-to/cross-platform/server/)
- [Playwright vs Puppeteer PDF 生成性能对比（腾讯云）](https://developer.cloud.tencent.com/article/2556037?policyId=1003)
- [HTML 转 PDF 技术方案调研（掘金）](https://juejin.cn/post/7338259035043561498)
- [WeasyPrint 自动生成 PDF 报告实践](https://textdata.cn/blog/weasyprint_auto_report/)
- [导出 PDF 三种方案对比（知乎）](https://zhuanlan.zhihu.com/p/2068085466700706372)

---

## 十一、M7 增量设计与验收（功能点全量补齐）

> 本章为 M7 追加章节（前文未改动）。M1–M6 阶段，录入类与管理类功能以 `deploy/seed.sql` 预置数据替代；
> M7 依据「功能点 §1–§13、§16 全部真正实现」的决定，补齐全部写路径与管理界面，使平台从"读得对"变为"写得全"。

### 11.1 交付清单

| 层 | 内容 |
|----|------|
| 管理端 API | AdminUserController（账号/任课 CRUD+重置密码/停用）、AdminOrgController（年级/班级/学生档案 CRUD+删除守卫）、AdminTermController（学期 CRUD+当前学期单活）、AdminIndicatorController（九格只读+二级指标 CRUD+引用守卫）、AdminTemplateController（模板 CRUD+启用锁）、MetaController 增补 `/grades /grids /indicators /subjects` |
| 业务写路径 | EvaluationService（评价引擎写穿链）、ScoreService（成绩录入/排名/Excel）、ComprehensiveController（五维综评）、ActivityController 封面上传、ReportController `generate-grade` 年级批量 |
| 前端 | 新页：成绩管理 /scores、日常评价 /evaluate、成长总结 /summary、综合素质 /comprehensive、系统管理 /admin（六标签）；改造：报告列表（管理员 班级/全年级 切换）、活动管理（封面上传+缩略图）、顶部导航 |

### 11.2 评价引擎写穿链（§5/§6）

报告只读预聚合表（Java 无聚合代码），故一次评价按固定顺序写穿 6 个目标：

```
① t_evaluation（明细）
② t_grid_stat_term upsert（points+=s、eval_count+=1、score+=s、kind_count 重算）
③ t_grid_stat_week upsert（weekNo = ⌊(evalTime−开学日)/7天⌋+1）
④ t_coin_week（仅本人 in_mine += s；in_class/in_grade 为全组共现值，写入会波及他人报告）
⑤ 能量币流水+账户（module=格名-指标名，display_order=99 不进收入 TOP5 榜首屏）
⑥ 班/年级九格均值增量平移 += s/班人数、+= s/年级人数（scale 4 HALF_UP）
```

**口径与 ReportDataBuilder 逐条对齐**：学期窗口 `[start 00:00, end 00:00]` 闭区间（学期末日白天不可录，录入接口 400 拒绝）；kind_count = `(title + " " + 指标名)` 去重组数（与记录卡分组同构）；周号与种子口径一致。

**无事务声明**：6 个写目标非原子（沿用本项目"控制器直写+无 @Transactional"惯例）。学校单机低并发场景下，部分失败的残差由后续重录评价的 upsert 语义自然收敛。

### 11.3 与既有契约的两处有据偏差（重要）

1. **ReportDataBuilder 追加 id 决胜序（1 行）**：`ORDER BY eval_time` 在 3796 条评价（大量同刻）下平局顺序依赖优化器计划；数据库 dump/restore 的 DDL 变更曾使「登记人/记录组」顺序漂移导致契约验证失败。追加 `ORDER BY id` 决胜后顺序确定性成立，且 DB id 序与 golden 序一致（种子按 golden 顺序插入）。这是对「ReportDataBuilder 零改动」红线的最小偏离——以一行追加保住"契约零漂移"的本意，无需重灌种子。
2. **gradeAvg 年级共享语义**：初一(1)班与初一(2)班同属 grade_id=1，`t_grade_grid_avg` 按（年级,学期,格）共享。对任何同学级学生写评价，学生1 报告中的 `cur.gradeAvg` 必然平移 s/52——这是聚合表设计使然，非污染。验证策略：写入期间断言「学生1 除 gradeAvg 外零差异」，测试后以 mysqldump 快照恢复，再跑全量契约验证兜底。

另有一处**种子数据归一**：种子 kind_count 含生成器抖动（不可从 t_evaluation 复现，如学生52 智格存 24 而记录卡仅 22 组）。写入路径的 kind_count 重算按「记录卡同构口径」执行，故首次写入该生该格后 `kinds == 记录卡数` 恒成立（自洽且幂等）；学生1 零写入，其契约值（含历史抖动）原样保留。

### 11.4 权限三档（有意差异，验证脚本显式断言）

| 写操作 | 权限 | 依据 |
|--------|------|------|
| 活动/荣誉/综评/寄语确认 | ADMIN + **本班班主任**（checkClassOperable） | 班级事务由班主任负责 |
| 日常评价 | **数据可见即可评**（checkStudentAccess，任课教师可评所教班学生） | 功能点 §1/§5：任课教师日常打分 |
| 成绩录入 | ADMIN + **t_teach 匹配的（班,学科）任课教师**（班主任非任课不可录） | 功能点 §3：按学科负责制 |
| 成绩考试/模板/指标/组织/学期管理、年级批量报告 | 仅 ADMIN | 管理事务 |

### 11.5 成绩与排名（§3）

- 录入即重算：每（考试,学科）全体竞争排名（同分同名次 1,2,2,4），class_rank 限班内、grade_rank 全体，Java 内存计算零自定义 SQL；排名列不进报告契约，零契约风险。
- 单列 max 结构：`t_exam_subject.class/grade_max` 与 `t_exam.class/grade_max_total` 回填时班级最高取全体最高（单列无法区分），文档注明，前端语义展示为"最高分"。
- Excel 采用 **Apache POI 5.3.0**（org.apache.poi:poi-ooxml），非前文 §五 设想的 EasyExcel：EasyExcel 3.x 绑定 POI 4.1.2/JDK11 时代且社区停维护，POI 直用无版本冲突——此为对第五章映射表的具体技术替换，特此记录。

### 11.6 管理守卫与模板锁

- 删除守卫：年级有班、班有学生、学生有成长数据（评价/成绩/参与/综评/寄语/荣誉）、学期有考试、指标被评价引用、活动有参与记录 → 一律 400 拒删。
- **指标改名/删除守卫**：被 t_evaluation 引用的指标改名会改写既有报告分组（记录卡键=标题+指标名）→ 400。
- **启用模板锁**：启用中模板（id=1）PUT/DELETE/状态切换一律 400「契约基线锁定只读」；草稿模板自由 CRUD；草稿→启用仅 DBA 人工执行（启用即契约基线）。
- 账号守卫：不可自删/自停/自改角色。

### 11.7 M7 验收（verify_m7.py + verify_m7_web.mjs）

断言组：①账号全生命周期（建→登录→重置密码→停用→拒登录）+组织/学期/指标/模板 CRUD 与守卫、非 ADMIN 全 403；②成绩：排名（含同分）、跨学科/跨班 403、模板下载、导入含 skip、幂等；③评价引擎全链 diff（写学生52：九格/雷达/周曲线/能量币/记录卡/班年级均值）+权限不对称；④综评三组 final 规则+报告反映；⑤封面链路+年级批量 52 份跑通；⑥总结 API 四块；⑦零污染（学生1 除 gradeAvg 外零差异+快照恢复后全量契约 PASS）。Web 端桌面+手机双档三角色过新页。

### 11.8 学生照片（§2 补齐，2026-08-20 追加）

功能点 §2 基本信息含"学生照片"，M7 交付时字段（t_student.photo_url）已在而存取路径缺，此处补齐，设计完全复用 §11.1 活动封面模式（MinIO + multipart）：

- `POST /api/admin/student/{id}/photo`（ADMIN）：jpg/jpeg/png ≤5MB，对象名 `student/{id}/{uuid}.{ext}`；重复上传先删旧对象再传新的；学生删除（deleteStudent）连带清理照片对象。
- `GET /api/admin/student/{id}/photo`（ADMIN）：返回字节流，扩展名定 Content-Type（.png→image/png 否则 image/jpeg）；未上传 404。
- **照片只进学生档案管理（StudentTab 列表缩略图+编辑弹窗上传），不进 PDF 渲染**——渲染模板/ReportDataBuilder 零改动，50 页契约不受影响。
- 前端：StudentTab 列表"照片"列 el-image 缩略图（fetchBlob→objectURL，仅本页 20 条），编辑弹窗选文件保存时 apiForm 上传。
- 验证：verify_m7.py 追加 6 断言（上传前缀/覆盖换对象/GET 字节一致/非图片 400/教师 403/未上传 404），89/89；verify_m7_web 42/42 复跑无回归。

### 11.9 前端风格统一（数智成长主题，2026-08-20 追加）

- 主题：站名「数智成长 · 初中素质报告平台」（通用式，不嵌校名）；靛蓝 #4F46E5 主色 + 青绿 #10B981 成长/正向语义色；logo、主按钮、标签页指示条、登录页等关键视觉走靛蓝→青绿渐变；气质定位「专业 + 活泼 + 青春」。
- 实现方式：**纯 CSS + Vue `<transition>`，零新依赖**。核心是 src/style.css 全局设计令牌层（令牌/基础/骨架/EP 覆写/动效/平板/手机/reduced-motion 八节），通过 Element Plus CSS 变量梯子（--el-color-primary-light-3/5/7/8/9、dark-2 等全套实算色阶）令全部 EP 组件整体换装，13 视图 + 6 管理页签零逻辑改动自动继承。
- 改动面：style.css（12→约 190 行）；App.vue（深靛蓝渐变 sticky 页头 + 内联 SVG 幼苗品牌 + 导航渐变下划线动效 + 路由过渡包裹）；LoginView（渐变 hero + 浮动光球 + 毛玻璃卡片）；index.html（title/theme-color/data-URI favicon）；main.ts（@element-plus/icons-vue 全局注册 4 行）；Summary/Timeline/Evaluate/Admin/Activity 五视图加 page-title 及局部着色；PreviewView .frame 润色。
- **E2E 契约不动**：94 断言依赖的导航/按钮/占位符/卡片头/标签/toast 文本、工具栏 el-select DOM 顺序（nth 0/1/2）、EP 原生 class 钩子、390px 无横向滚动全部逐字保留；品牌文本不在断言内故可改名。
- **路由过渡离场必须零时长**（重要教训）：`<transition mode="out-in">` 离场期间旧路由仍留在 DOM（原 160ms），E2E「waitForURL → waitForSelector('.el-select') → 立即 nth(1).click()」会命中**旧页面**的 select——点击落在即将卸载的组件上，其弹出层随之销毁 → 等待下拉项 30s 超时（曾确定性复现）。`.page-fade-leave-active{transition:none}` 令旧路由即刻卸载；任何非零离场时长都会给「共享选择器 + 立即点击」类测试留下竞态窗口。入场 fade+rise 动效保留，视觉几乎无损。
- 验证：npm run build 零 TS 错；verify_web 32/32、verify_m6_web 20/20、verify_m7_web 42/42（首跑 1 条时序抖动，复跑两次全绿）= **94 断言全 PASS**。

### 11.10 模板原版图片嵌入 + 打印第 6 页页脚重叠修复（2026-08-20 追加）

- 背景（用户两问题）：①打印第 6 页（=物理 p04 九格页）底部页码/细线与图片重叠——根因：p04 用 9 个 128pt CSS 圆三行排布（top:400pt 起 → 底缘 824pt）压过页码区（pgnum 810pt / pline 814.6pt）；②前八页图片缺失——原版模板（学生成长报告册.pdf）这些页本有图片，应从模板原样搬运而非另传。
- 方案：fitz 从原版 PDF 提取图片，**smask 软蒙版必须手工合并为 alpha**（extract_image 不带蒙版；10/14 张带）。照片/整页底图（alpha 近全不透明）平掉 alpha 压 JPEG q82-85，真透明元素保 RGBA PNG；源图 6–10 倍过采样（如整页底 5950×8416），降至 ~300dpi 视觉无损。产物 12 张共 1.9MB 存 `report-renderer/src/main/resources/static/img/`；RenderPdf.java 读取后 **base64 data URI 内联为模板变量**（imgIdeaBg/imgPhoto1/imgPhoto2/imgNineGrid/imgPrincipal/imgCornerTl/imgCornerBr/imgDeco/imgCornerTr/imgIconL/imgIconR/imgLogo，与 echartsJs 同模式规避 file:// 相对路径）；report.html 按原版 bbox（PDF pt ↔ CSS pt 1:1）绝对定位引用——全部 abs 定位不占文本流，50 页标题序列/聚合 JSON 零影响。
- 逐页落位：p02 理念底图（纵向渐变+底部装饰带，梯度分析证实 776–842pt 有花纹故整图嵌入；目录底/风采底为纯渐变 CSS 等价不嵌）；p03 两张照片 (136,271)/(136,516) 替换虚线占位 + 标题两侧小图标；p04 九格插画 (136,293)-(459,672) 替换 9 个 CSS 圆（**修复重叠**：底缘 672pt 远离 810pt）+ 标题图标；p05 校长寄语 = 原版整页图直接铺（原版该页全部内容包括寄语文字烧在图里，属学校级静态内容；模板本就只有占位框、无数据字段，故无文字冲突）；p06 左上/右下大角饰；p07 与 p08–18（学科页 th:each）右上角饰+小装饰；**页眉 logo 由页眉 JS 注入器与文字同侧注入全部内容页**（奇数页左 395pt/偶数页左 22pt——与 .hdr 的 r/l 规则及原版六页实测逐页吻合，.hdr.l 的 left:56pt 正是给左 logo 留位；nohdr 页自动跳过）。
- 边界决策：p01 目录渲染与底图残差仅 1.9%（内联"图"无可见贡献）不动；p07 内联吉祥物插画 (126,183)-(222,284) 与我方 echarts 雷达图区重叠 → 舍（图表由数据重绘，不能被遮）；其余隔页（p20/p41/p43/p47）角饰超出"前八页"范围且配色属蓝/绿区未嵌。
- 部署：renderer 改动只需 `report-renderer mvn package`（RenderService 每渲染 fork 新 JVM，自动生效，服务端免重启）。
- 验证：样例 PDF 50 页；逐区域像素检查（照片/插画/角饰/logo 的非白占比与方差）全部命中；打印第 6 页 790–840pt 页脚带非白仅 1.5%（仅页码与细线，重叠消除）；verify_contract.py PASS（聚合 JSON 全等 + 50 页标题序列逐页一致）；verify_web 32/32（含服务端批量真实渲染、下载 PDF 2.76MB）+ verify_m6_web 20/20 + verify_m7_web 42/42 = **94 断言全 PASS**；`docs/img_embed_check/` 留原版 vs 生成并排对比图 8 张供目检。

### 11.11 一键容器化交付（2026-08-22 追加）

- 目标：交付对象从"本机能跑"升级为 `docker compose up -d --build` 五容器（mysql/redis/minio/report-server/report-web），任装 Docker 的机器可复现。
- 形态：单 Dockerfile 双 target——`server`（eclipse-temurin:21-jre + server jar + renderer classpath/target/lib 与中文字体，渲染 fork JVM 机制不变）与 `web`（node 构建 → nginx:alpine 托管 dist 并反代 /api → server）。server 镜像 ~5.5GB（Playwright+字体），web ~50MB。
- AI 配置走 override 文件模式：`docker-compose.override.yml.example` 入库为模板（真实 key 只存本机 gitignore 的 override），Spring 宽松绑定 `AISCHOOL_AI_*` 环境变量；三项留空 = 未启用 AI 降级模板，开箱可跑。
- 决策：不引入 K8s/注册表等多机编排——单机单校规模下 compose 已满足，复杂度最小。
- 验证：五容器 healthy；94 项 E2E 指向 80 端口全 PASS；真实 key 下 3 类 AI 功能（寄语/总结/证书识别）均 source=llm。

### 11.12 AI 分析任务化（t_ai_task 队列，2026-08-22 追加）

- 需求：寄语/总结生成改为后台任务——切页/关浏览器不中断、多任务并行、班主任可整班批量、多位老师同时使用不互相干扰；并发须贴合 LLM 供应商限额（防 429）。
- 方案：沿用报告渲染已验证的任务队列模式（DB 事实表 + worker 线程池），不换技术栈。`t_ai_task`（type=COMMENT/SUMMARY，状态 排队/生成中/成功/失败，结果 JSON 落 result_json）；`AiTaskService` 持 `ThreadPoolExecutor(16, queue 10000)`；提交即返回 taskId（同学生同类型未完成去重复用），前端 Pinia 轮询 `/api/ai/tasks/mine`（2s）驱动全局面板与页面回填；服务重启 @PostConstruct 把中断任务重置回排队重新入队。
- 并发定 16 的依据：DeepSeek 官方并发上限 flash 2500/账号、pro 500/账号，超限仅 429 不封号；16 = flash 的 0.6%，吞吐 ≈1 任务/秒（实测 17 任务 15 秒），5 位老师同时各批量 50 人 ≈2 分钟；`AISCHOOL_AI_CONCURRENCY` 环境变量可调不改编码。**所有老师共用同一队列与 worker 池**——并发不随教师数放大，天然全局限流。
- 隔离：mine 按 created_by 过滤（面板只见自己的）；详情按学生数据权限校验（checkReadable）；提交须 ADMIN 或该班班主任（checkWritable）。
- 恢复语义：寄语草稿本就落 t_comment.ai_draft（切页/重开浏览器后 load() 回显）；总结不落库，恢复走任务 result_json。前端 byKey 索引取"每学生×类型×学期最新任务"。
- 坑与修（实测发现）：①LLM 输出 Markdown 前缀/标题独占一行 → parseBlocks 改跨行收集 + 提示词禁 Markdown + stripMd 清洗；②LLM 慢响应实测可达 61s → 读超时 60s→120s，超时降级模板任务不失败；③前端 byKey 曾留最旧任务导致新结果不渲染（降序列表先占不覆盖修复）。
- 验证：API 9/9（去重/并发上限 16/排队位次/落库/隔离/真实 llm）；UX 12/12（切页后台跑、恢复、批量+面板 18 行、badge、浏览器重开恢复、console 0 错）；E2E 94/94（AI 关闭基线，寄语断言改为任务版）。

### 11.13 运维与扩容批次 + 交付收尾（2026-08-22 追加）

- 背景与范围：交付评审明确"后期可维护性/扩容性必须好"，四批落地——A 学生流转、B 运维基线、C 观测与开放、E 前端视觉简约；随后容器化构建切国内源、交付清理。
- **A 学生流转（零 SQL、全管理界面）**：`POST /api/admin/student/import` 新生 Excel 批量导入（逐行校验、部分成功语义、问题行返回行号+原因，配套模板端点）；`PUT /api/admin/class/{id}/move-students` 整班调班（升年级/分班——历史数据按「学生+学期」关联，搬班零影响）；`PUT /api/admin/class/{id}/mark-status` 整班标记毕业/转出（换届时整届一键退场，档案与报告保留可查）；`GET /api/student/list` 收紧为**仅返回在读**（日常业务不见退场学生），管理端 list 增 `status` 筛选查历史届。
- **B 运维基线**：Flyway（`db/migration/` V1=基线（与 schema.sql 一致）+ V2 审计表与 AI token 字段；`baseline-on-migrate` 令存量库无感接入；后续升级只追加 V{n}、禁改历史）；AuditFilter 全部写操作留痕（t_audit_log：用户/接口/参数摘要/结果码/IP，管理端分页查询，留 180 天）；登录防爆破（Redis 失败计数 → 锁定期 429 提示等待分钟数，自动解锁）；五容器日志统一 json-file 50MB×3；`deploy/backup.sh`（mysqldump + MinIO 数据 → backups/，建议 cron）；RetentionService 每日 03:17 清理（t_ai_task 留 90 天、审计 180 天；**副作用**：总结结果只在任务 result_json，90 天后历史总结详情不可查——寄语在 t_comment、报告 PDF 在 MinIO 均不受影响；保留期 `aischool.retention.*` 可调）。
- **C 观测与开放**：`GET /api/admin/ai/usage` AI 用量聚合（token 随 t_ai_task 落库，byDay/byTeacher 两维，前端系统管理「AI 用量」Tab）；提示词外置 `aischool.ai.prompts.*`（system-teacher/comment-instruction/summary-instruction，留空用内置默认，调话术不改代码）；springdoc-openapi Swagger UI（仅暴露 /api/**，生产 `SPRINGDOC_API_DOCS_ENABLED=false` 一键关闭）；成绩单/寄语 xlsx 导出（POI，复用导入列结构）；GitHub Actions CI（推送/PR 到 main：后端 mvn package + 前端 npm build，不跑需栈的接口测试）。
- **E 前端视觉简约化**：按钮/卡片细边框+浅中性阴影、Tab 下划线选中态、表单控件统一；motion-v 弹性动效（登录卡 spring 入场、页头 scroll-progress 进度条，尊重系统"减弱动态效果"）；手机端无横向滚动复核。E2E 依赖的 DOM 契约（选择器/文本/顺序）零改动。
- **容器化国内源修正**：fonts 层 apt 切 mirrors.aliyun.com（sed 同时覆盖 noble 的 deb822 `ubuntu.sources` 与旧 `sources.list`，其一不存在由 `|| true` 兜底）；npm 走 npmmirror、Maven 走 settings.xml 阿里云镜像——国内全量构建不再卡国外源（修正前 61MB 字体包从 archive.ubuntu.com 拉取超时）。
- **tools/m2-repo 不入库决策**：129MB 本机依赖缓存 gitignore 只留 .gitkeep 占位——仓库保持轻量、国内推 GitHub 可靠；克隆者首次构建由阿里云全量解析（一次性较慢，之后层缓存）。Dockerfile 的 COPY 对空目录同样成立（仅复制 .gitkeep，无害），构建逻辑不因缺缓存而失败。
- 验证：D2 裸跑全量（回归 10/10 含契约零漂移；新功能冒烟：AI 用量聚合/成绩与寄语 xlsx/新生导入/Swagger/防爆破 429 全过）+ D3 容器（nginx:80 端到端、容器内单份渲染 4.86MB %PDF、首页中文字体栅格化目检无方块、AI 用量 Tab 真实 tokens 渲染 0 console 错）；三视口 E2E 无横向滚动。
- 交付清理（本机痕迹，不影响 fork 复现）：删可再生产物——D 盘 target×2/dist/node_modules/shots（~680MB，全部 gitignored）、C 盘 .m2 冗余目录与 ms-playwright 旧版本、本项目 docker 镜像/数据卷/构建缓存（~9GB）；`docker compose up -d --build` 可从零完整复现（数据重新 seed）。


### 11.14 平台整体换校：东华 → 石实实验学校（2026-08-23 追加，shishi 分支）

**目标**：示例学校由「东莞市东华初级中学」整体换为「佛山市南海区石实实验学校」（原石门实验学校，1999 创办），东华内容全部删除；平台文案「初中」改「中学」；前端加石实元素。泛化（东华）状态保留在 git 历史，后续迁移第三所学校可从历史切出。

**改动清单**（与替换相关的全部，代码与种子零东华残留）：

1. **数据面**（单一事实源 golden，链路 golden → seed → DB → 聚合 → 渲染）：
   - `report-renderer/scripts/expand_golden.py`（golden 的生成器，改这里不改 JSON）：school.name/intro/nineGridIntro、philosophy 六条（校训「任重道远，毋忘奋斗」/理念「扬长教育，出彩人生」/育人追求/共同价值观/石门精神/办学使命）、radar motto 换《论语·泰伯》「士不可以不弘毅，任重而道远」；radar max/coin 刻度不动（模板坐标轴复刻依赖）。
   - 重跑 `expand_golden.py` 生成 golden → 重跑 `report-server/scripts/seed_db.py` 生成 `deploy/seed.sql`（模板行由 golden.school 逆映射，东华行自动替换为石实行，seed_db.py 零改动）→ `_m7_snapshot.sql` 由 verify_m7 运行时自刷新后提交。
2. **渲染器**：
   - `report.html` 页眉改为数据驱动：`DATA.school.name + '学生成长报告册'`（原硬编码东华）。
   - 静态图 4 张替换（素材源：百度百科校门/庭院照，`shishi-assets/` 中转不入库）：`img_photo1.jpg`（校门，3:2 中心裁 559×373）、`img_photo2.jpg`（庭院 500×333）、`img_logo.png`（设计生成椭圆校徽 492×424 透明底：深蓝双环+金内环+上弧校名+中央「石实」+1999，匹配页眉 27×22pt 椭圆槽位与原东华徽同为椭圆避免变形）、`img_principal.png`（校长寄语整页 1414×2000，HTML 版式 Chrome headless 截图；无照片/手写签名素材，改用校徽水印+校名落款）。
3. **前端**（`report-web`）：title/brand/登录页文案 → 「石实实验学校 · 中学素质报告平台」、副标语校训；`public/` 新增 badge.png/gate.jpg/favicon.png；登录页 ≥900px 左侧校门照片面板（校名+校训+英文，窄屏隐藏）；顶栏 brand-mark 与登录 logo 换校徽图（白底衬深色导航）；favicon 换简版徽。E2E 零品牌文本断言，不受影响。
4. **文档**：README.md / 交付文档.md「初中」→「中学」；本节即换校记录。历史章节（§11.9 等）与 ACCEPTANCE.md 属当时验收记录，按只追加铁律不追溯改写。

**契约基线再生成**（`**/target/` 不入库，基线 PDF 属本机验证产物）：

```bash
# 1) 起新栈并导入新种子（或空库首启自动 seed）
docker compose up -d --build
docker exec -i aischool-mysql mysql -uroot -paischool123 ai_school < deploy/seed.sql
# 2) 跑契约（渲染器需先 mvn package 出 target/classes+lib）
cd report-server && PYTHONIOENCODING=utf-8 python scripts/verify_contract.py
# 3) 确认新 PDF 内容正确后刷新本地基线
cp report-server/target/contract-check/agg.pdf ../report-renderer/target/report.pdf
# 4) 重跑 verify_contract.py 应双 PASS（① 聚合≡golden ② 50 页标题序列≡基线）
```

**迁移下一所学校 SOP**：改 expand_golden.py 的 school 块（intro/nineGridIntro/philosophy 六条/motto，长度与东华/石实同量级防溢出）→ 重跑 expand_golden.py + seed_db.py + 导库 → 替换 4 张静态图（照片 3:2、徽 492×424 椭圆、寄语页 1414×2000）→ 前端 public/ 三资产与文案 → 刷新基线 PDF → verify_contract / verify_m6 / verify_m7 全绿。

### 11.15 「五育·扬长」九维改名 + 全站虚化背景 + 前端 VI 配色（2026-08-24 追加，shishi 分支）

**#25 九维改名**（单一事实源：`report-renderer/scripts/expand_golden.py` 的 `GRIDS`；**位置/顺序绝不能变**——统计与雷达按位置对齐，t_grid 的 id/sort 不动）：

| 原名（位序） | 新名 | 附带指标改名 |
|---|---|---|
| 国格 | 品德修养 | — |
| 信格 | 行为习惯 | 副标题「行为习惯-」（空名占位） |
| 心格 | 身心健康 | — |
| 品格 | 劳动实践 | 礼貌礼节 → 劳动习惯 |
| 智格 | 学业水平 | — |
| 慧格 | 实践创新 | — |
| 美格 | 艺术素养 | 艺术素养 → 艺术测评 |
| 特格 | 特长扬长 | — |
| 体格 | 体育锻炼 | 体育锻炼 → 体育训练 |

改名链（全部从 GRIDS 传导，无第二数据源）：`expand_golden.py` → `golden_student.json` → `seed_db.py`（注释口径 + 重跑生成 `deploy/seed.sql`）→ `report.html` p04（目录/注释/标题改「"扬长"育人体系」）→ `static/img/img_nine_grid.png` 重生成（HTML+Chrome headless 截图 972×1140@2x，黑底、中心金环「扬长教育/出彩人生」、九圆环顺时针新名、底部红字；HTML 中间产物在 shishi-assets/，不入库）→ Java 文案（EvaluationService/AdminIndicatorController/AiDraftService/RuleFactsService/MetaController）→ Vue（AdminView 页签「九格指标」→「育人指标」、IndicatorTab/EvaluateView 标签）→ 验证脚本（verify_m7_web.mjs 页签名、verify_m7.py 断言 `"智" in g["name"]` → `"学业" in g["name"]` 两处）→ README/接口文档口径。

- **UI 术语口径**：泛指维度用「九维」；管理页签用「育人指标」；p04 版块名「"扬长"育人体系」。
- **AI facts 键同步**（双处必须同时改）：`RuleFactsService.facts.put("九格表现"→"综合素质表现")` ↔ `AiDraftService` 两处 `facts.get(...)`。
- **有意不改**：`V1__baseline.sql` 表注释仍是「九格」旧称——Flyway 已应用，改注释会破坏迁移 checksum；`report.html` 内 `国格` 等为 Thymeleaf 静态回退占位，渲染恒被数据替换。
- **契约基线再生成**（改名后必做一次，本地 target/ 不入库）：
  ```
  # 1) 渲染学生1 得 report-server/target/contract-check/agg.pdf
  # 2) cp report-server/target/contract-check/agg.pdf report-renderer/target/report.pdf
  # 3) 重跑 cd report-server && PYTHONIOENCODING=utf-8 python scripts/verify_contract.py → ①②双 PASS
  ```

**#28 全站虚化背景**：`App.vue` 新增 `.app-bg`（fixed、z-index:-1、`public/campus-bg.jpg` + 白纱渐变叠加），页脚改半透明胶囊，全站页面统一虚化校园底图（不影响 E2E 与打印）。

**#29 前端 VI 配色**（石实红系，仅前端，PDF 渲染器 img_logo.png 有意不改）：主色石实红 `#a8232b` + AI 蓝 `#0078d4` + 点缀金 `#c98a2d`；落地：`style.css` 设计令牌/EP 组件色阶/阴影，`App.vue` 导航深枣红 `#4a1216` + 金色下划线，`LoginView` 红渐变，`AiTaskPanel`/`EvaluateView` 同步；`badge.png`/`favicon.png` 重生成（石实红底+金穗）。

### 11.16 登录页虚化背景 + 契约退出码修复（2026-08-24 追加，shishi 分支）

- **#30 登录页背景**：登录页原为纯红渐变，与全站虚化底图（#28）不一致。`LoginView.vue` 改 `.login-wrap::before` 承载 `public/login-bg.jpg`（石实校园环形教学楼庭院，**与左侧面板 gate.jpg 校门不同景**），`inset:-48px` 外扩补偿 blur(22px) 边缘褪色，叠石实红/AI 蓝双 radial 品牌色罩保证白卡对比度；`.login-split` 提升 `z-index:1`（定位过的 ::before 会盖住静态兄弟）；图片加载前深红 `#3a0d10` 兜底。三视口 QA（qa_tiers）+ 目检双 PASS。
- **#31 verify_contract 退出码**：原脚本打印 `RESULT: FAIL` 时仍 `sys.exit(0)`，使 verify_m7 内嵌契约门形同虚设（CI 不跑 verify_*.py，故线上 CI 无影响）。修复为退出码与 RESULT 一致后重跑 verify_m7：89/89 PASS，内嵌契约门首次真实生效。

### 11.17 PDF 页码/封面修复 + 旧校对照截图删除（2026-08-24 追加）

- **背景**：fork 用户反馈三类问题（在其 fork 中所见）：残留旧校元素、PDF 页码偏移、缺封面。排查结论：fork 于 8-24 换校推送**之前**切走（当前 main 本地渲染 51 页零旧称，PDF 文本层/模板/seed/golden 全查），页码为真实缺陷（页脚打印号 = 物理页 + 2），封面缺失属实。
- **修复**（`report.html`）：① 页码注入改为打印号=物理页，页眉左右侧沿用原排版（side 按加封面前旧页号计算）；② 目录重编为 3,4,5,6,7,20,21,42,44,48,50；③ 新增 p1 封面（nohdr nonum，青金主色与目录页同系：双线金框、校徽、校名、书名"学生成长报告册"、校训、姓名/班级/学号信息卡，全部数据驱动），总页数 50→51；④ 学校理念页 Thymeleaf 静态占位符由旧校文案换为石实理念行（永不渲染，纯源码观感）。理念六行（校训/办学理念/育人追求/共同价值观/石门精神/办学使命）已经官网与权威媒体逐条核实，无需留空。
- **配套**：`verify_contract.py` 页数断言 50→51；契约基线 `report-renderer/target/report.pdf` 由新模板重渲刷新（标题序列 sig 一致性验证过）。
- **删除**：应用户要求删除 `docs/sample_pages/`（52 张原版模板逐页参考图）与 `docs/img_embed_check/`（6 张原版 vs 生成并排对比图）——均为 8-22 交付快照提交的换校前内容，fork 用户翻仓库可见；git 历史仍可找回。§11.10 等历史段落中上述路径引用随之失效（本文档只追加，原文不改）；`deploy/ACCEPTANCE.md` 目检材料行、`交付文档.md` 交付清单两行已同步移除。本地 gitignored 的《学生成长报告册.pdf》原件不受影响。
- **验证**（本地渲染 `target/inspect.pdf` 全套过）：51 页；页脚=物理页 p4–p50 逐页全对；目录印刷数字与指向页内容实测一致；封面几何+像素级检查（居中/无重叠/四边金线/渐变/信息卡）通过；页眉 42/47（5 张章节扉页按设计无页眉）；图表页落版正常；全 51 页广谱关键词零残留。
