# 上线预演与回滚预案（v1.1.0 / versionCode 19）

> 内部文档。服务器地址与账号见 `local/creds.md`（gitignored），本文不写。
> 红线：上线当天执行前向用户报备、同意后才执行；本预案先演练、后上线。

## 一、版本概况

| 项 | 值 |
|---|---|
| versionName | 1.1.0 |
| versionCode | 19（生产当前 latest=18 / v1.0.17） |
| 覆盖内容 | 石实SHINE 重构批2-26 全部：领导端、家长端（注册/报告/荣誉/大事记）、成长银行、行政办公五件套（公章/物资/请假/场地/报修）、谈心、教师足迹、报告三类（学期/学年/在校）、名册导入（宿舍三列）、邀请码绑定、数据体检、批量操作、官方校徽+App 更名石实SHINE、审查批 17 项修复 |
| 数据库迁移 | V15 → V27（全部为加表/加列，向后兼容，支持镜像回滚） |

## 二、上线顺序（关键：服务器先、APK 后）

App 页面全部来自服务器（WebView 即网页），**服务器部署完成 = 全体老师立即用上新功能，无需等 APK**。APK（19）只承载原生层：App 名称「石实SHINE」、官方校徽图标、启动屏、版本号（用于触发升级弹窗）。

1. **预检**：`bash deploy/backup.sh` 手动做一次上线前备份（MySQL + MinIO），确认备份文件落盘；
2. **服务器更新**（服务器项目目录）：
   ```bash
   git pull
   docker compose -f docker-compose.yml -f docker-compose.server.yml -f docker-compose.override.yml up -d --build
   ```
   （服务器未配 AI override 时去掉最后一个 `-f`）
3. **服务器验证**：`docker compose ps` 五容器 healthy；浏览器登录管理端跑一遍数据体检；`/api/app/latest` 仍返回 code 18；
4. **发 APK**：`python local/pub_release.py <apk> 1.1.0 19 "更新说明逐行"` → 老师 App 自动弹升级，App 内下载安装；
5. **发后验证**：`/api/app/latest` 返回 code 19；真机装 19 开 App 验证名称/图标/启动屏。

## 三、上线前预演清单（本地）

| # | 项 | 结果 |
|---|---|---|
| 1 | 本地 5 容器跑批26 代码全链冒烟（UI 10/10+回归 19/19×2+批量 54/905） | ✅ 已过（批26 QA） |
| 2 | APK 1.1.0/19 构建签名（web build + cap sync + assembleRelease） | ✅ 9-26 构建成功（8.6MB，存 local/release/shishi-shine-v1.1.0.apk） |
| 3 | 生产备份拉取 → 本地临时库恢复 → 批26 镜像跑迁移 → 老数据回归 | ✅ 9-26 演练通过（见下「演练记录」） |
| 4 | 版本号元数据核验 | ✅ output-metadata=1.1.0/19；apksigner V2 签名有效，证书 SHA-256 与线上 v1.0.17 完全一致（升级链安全） |

### 演练记录（9-26，生产数据迁移演练）

- 生产现状：五容器 healthy（Up 8 天），commit=afa6f2b（9-16 版），schema=**V8**；每日自动备份正常（MySQL 245K + MinIO 232M）；
- 拉取当日备份 `ai_school_20260926_031701.sql.gz` → 本地临时 MySQL（独立容器/网络，不碰本地测试栈）恢复；
- 批26 镜像启动：**flyway V9→V27 共 19 个迁移一次全过**（0.7s，仅一条 MySQL 8 VALUES() 弃用 WARN 无害），server 5.4s 正常启动（未配 MinIO 不拦启动）；
- 老数据回归：t_user 19 / t_student 52 / t_report 63 行完好；生产 admin 账号登录成功（token 正常签发）；
- 演练后临时容器/网络全清。**结论：生产数据 + 批26 代码 = 无损迁移，上线链安全。**

## 四、回滚预案

**触发条件**：上线后核心链路（登录 / 评价 / 报告 / 家长端）不可用且 30 分钟内未定位根因。

**服务器回滚**（页面立即回到旧版，优先执行）：

```bash
cd <服务器项目目录>
git checkout <旧版commit>          # 上线前记下 git rev-parse HEAD
docker compose -f docker-compose.yml -f docker-compose.server.yml -f docker-compose.override.yml up -d --build
```

- 数据层无需回滚：V15-V27 全部为加表/加列，旧代码不读新列，新 schema 跑旧镜像安全；
- 回滚后旧 APK 与新 APK（19）均可用——App 页面来自服务器，壳版本不影响。

**APK 侧处置**：

- 升级弹窗有问题：管理端「版本更新」页签点**撤回**，未升级的老师不再提示；
- 已装 19 的设备**不能降级安装**（Android versionCode 单向）；若 19 壳本身有缺陷，走紧急修复包 code 20。

**回滚后动作**：保留现场（容器日志 `docker compose logs --since 30m` 存档）、通知用户、定位修复后重新走上线流程。

## 五、上线当天检查单

- [x] 用户已报备同意（9-26 用户拍板直接上线）
- [x] backup.sh 备份落盘（ai_school_20260926_095528.sql.gz + minio-data_20260926_095528.tgz）
- [x] 记录上线前 `git rev-parse HEAD`（回滚锚点 **afa6f2b**）
- [x] 服务器 git pull（→6d734b1）+ up -d --build 五容器 healthy；flyway V9→V27 十九迁移全过
- [x] 正式名册导入（同批）：清 seed 测试数据（33 关联表，19 登录账号保留）→ 9 年级 94 班 → 487 师 → **4082 生零失败**；学号 1250101/2240101/2260101 与宿舍三列抽验过
- [x] pub_release.py 上传 19 号包（releaseId=5）
- [x] `/api/app/latest` = 1.1.0/19（notes UTF-8 正常，size 与本地一致）
- [x] 上线后验证：APK 下载/体检/荣誉墙/足迹/学生列表/首页全 200
- [ ] 真机升级验证（名称/图标/启动屏/登录）——交用户

### 上线过程实记（9-26）

- **踩坑①（已根治）**：学生导入 4082 行处理超 nginx `proxy_read_timeout 120s` → 504 返回 HTML 错误页、脚本 JSON 解析崩；本地直连 server 无此层故批8 未暴露。修复=nginx.conf 120s→600s（与导入脚本对齐），已提交推送并在生产 web 容器生效。
- **踩坑②（脚本适配）**：生产 mysql root 密码≠本地，导入脚本 mysql() 改走容器内 `$MYSQL_ROOT_PASSWORD`（backup.sh 同款）；BASE 用 127.0.0.1:18086。
- **踩坑③（小坑）**：脚本 login() `.get('data',{})` 在 data=null（密码错误响应）时返回 None 而非 {}——键存在值为 null 时默认值不生效，改 `(r.json().get('data') or {})`。
- 导入脚本第三轮（前两轮分别崩于③①）遇 SSH 长连接断开（10054）但服务器进程存活跑完，数据 4082/94/506 齐后清残留进程。
- 服务器留存：~/AI_school/local/roster_import_prod.py + roster_out/（正式名册产物，可重放）。
