# 数智成长平台 · 使用手册

面向学校老师与管理员的日常操作说明。平台有两种使用方式：**安卓 App**（安装包安装）和**浏览器**（打开学校服务器网址，功能相同，管理员建议用电脑浏览器）。

---

## 1. 登录与账号

### 账号从哪来？

系统**没有自助注册**——所有账号（包括管理员账号）都由管理员在「系统管理 → 教师与账号」里创建。这是有意的安全设计，避免校外人员注册进来看到学生数据。

| 角色 | 能做什么 |
|---|---|
| 管理员 | 全部功能 + 系统管理（建账号、建班级、导数据等） |
| 班主任 | 本班全部学生的全部操作（成绩、评价、报告……） |
| 任课教师 | 自己可见班级的操作 |

### 首次登录

1. 装好 App（或浏览器打开服务器网址），输入管理员发给你的**账号和初始密码**。
2. 用初始密码（管理员设置 / 重置 / 批量导入的 `Shishi@2026`）首次登录后，系统会**强制要求先改密码**（至少 8 位），改完才能使用其他功能。

> 忘记密码：找管理员在「系统管理 → 教师与任课 → 重置密码」，重置后首次登录同样会强制改密。
> 管理员自己忘了密码：用另一个管理员账号登录后，同样方式重置。
> 密码连错 5 次会锁定 10 分钟（防他人恶意尝试），等一会儿再登。

---

## 2. 管理员：系统管理

用管理员账号登录后进入「系统管理」，电脑浏览器操作最方便。

### 2.1 建教师账号

- **一个个建**：「教师与任课 → 新建账号」，填登录名、初始密码、姓名、角色、手机号。
- **批量导入**：「教师与任课 → 批量导入」：
  1. 点「下载模板」得到 Excel；
  2. 从第 2 行开始填：**账号、姓名、角色**必填（角色填 管理员/班主任/教师），其余选填（手机号、工号、性别、任教学科、职称、职务、教龄、入职年月、简介）；
  3. 班主任想直接指定带哪个班，填「班主任所带班级」列（班级名要与系统一致；该班已有班主任的行会跳过）；
  4. 导入后统一初始密码为 `Shishi@2026`；老师首次登录会被强制改密，无需逐个提醒。

导入是「部分成功」模式：合法行入库，问题行跳过并列出行号和原因，改完可再导。

### 2.2 建班级和学生

- 「年级与班级」：建年级、建班级（班级的班主任即在此指定，也可由批量导入带入）。
- 「学生」：单个新建，或「导入新生」Excel 批量导入（班级名称必须与系统完全一致）。
- **学年流转（升年级 / 分班 / 毕业）**：「年级与班级」支持**整班调班**——升年级时先建好新年级的班级，再把各班整班调入；转出/毕业的学生不动，历史成长数据全部保留。整届退场用「标记毕业」，档案与已生成报告保留可查。
- **新学期**：「学期」页签「新建学期」（名称 + 起止日期），勾选「设为当前学期」后，全平台（成绩、评价、报告）默认按新学期工作。

### 2.3 任课关系

「教师与任课 → 任课关系」：选择 教师 × 班级 × 学科 添加。任课关系决定任课教师能看到哪些班。

### 2.4 教师档案：总览与代填

「教师档案」页签可看到**全校所有教师**的档案一览：照片、姓名、角色、任教学科、职称、工号、手机，以及「已填 / 未填」标记。

- 点某行的「编辑」即可为该老师**代填或修改**档案（含上传照片），老师本人仍可在 App「我的 → 教师档案」自己维护；
- 老师还没填的会标「未填」，方便管理员/领导逐个补齐；
- 工号全校唯一，重复保存会被拒绝并提示已被谁占用。

管理员从**首页快捷功能「教师档案」**进入即直达本页签；普通老师点同一入口则进入自己的档案页维护。

### 2.5 其余管理页签一览

| 页签 | 用途 |
|---|---|
| 学期 | 新建/编辑学期、设为当前学期（新学年开局用，见 2.2） |
| 育人指标 | 查看与维护「五维评价」的指标体系 |
| 报告模板 | 查看当前报告册模板与锁定状态（模板版式调整需开发方配合） |
| 审计日志 | 谁在什么时间新增/修改/删除了什么，全量留痕可追溯 |
| AI 用量 | 开通 AI 服务后，按日 / 按教师的调用与 token 消耗统计（见 7.1） |

---

## 3. 成绩管理

### 3.1 新建考试

「成绩管理 → 新建考试」：选学期、填考试名称和日期、加科目并设置各科满分。**所有老师都可以建**（重复了可让管理员清理）。

### 3.2 录入 / 修改成绩

页头依次选「考试 → 班级 → 学科」，成绩单自动带出全班名册：

- 直接在表格里点分数格子修改，改完点**「批量保存」**——班级/年级排名自动计算；
- 空白 = 未录入；把分数**清空再保存 = 删除**该条成绩；
- 大批量：下载模板 → 填好分数 → 导入 Excel；已有成绩可「导出 Excel」留档。

### 3.3 谁能录成绩

- **本班班主任**：本班全部学科都能录；
- **任课教师**：自己任课的班/学科，以及档案里「任教学科」对应的学科；
- 其他情况只读（表格上方会显示「只读」标签）。

---

## 4. 成长报告

「成长报告」：选班级和学期，可单个生成或整班批量生成。管理员还可切「全年级」：选年级后列表自动汇总全年级所有班的学生（多一列班级），学期切换同样生效；「批量生成全年级」一次排队整个年级。生成是排队的（几秒到十几秒一份），进度在「通知（生成中心）」页看，失败的可以一键重试。

- **重新生成**：报告已生成的学生，行上有「重新生成」按钮——新录的奖项、微光、成绩会刷新进新版本；点前有确认提示，历史版本仍保留可追溯，列表始终显示最新一份。
- **预览**：网页版直接在线看；App 里会调起系统分享面板，选 WPS/浏览器等查看器即可看。
- **下载**：网页版直接存文件；App 里在分享面板选「保存到文件」。

报告册末尾（综合素质评定后）有**「学生改进方向」**专页：学业上与班级最高分差距最大的 3 个学科、综合素质低于班级人均最多的 2 个维度，各配努力建议，由系统按本学期数据自动生成，供新学期规划与家校沟通参考；有微光记录的学生其后还有一页「成长掠影」。

---

## 5. 日常记录

| 功能 | 入口 | 说明 |
|---|---|---|
| 微光信箱 | 首页/班级页「拍照」 | 拍下学生闪光瞬间，自动进班级页轮播、学生详情和报告；AI 可自动配一句点评（需学校开通 AI 服务） |
| 五维评价 | 评价 | 按指标给学生记分（+/-），支持写评语 |
| 成长寄语 | 寄语 | AI 起草 + 老师修改定稿（未开通 AI 时用模板起草），可导出 |
| 荣誉证书 | 荣誉 | 拍证书上传，AI 识别内容（未开通 AI 时手动填写） |
| 校园活动 | 活动 | 管理员创建活动 → 报名签到 → 录获奖与表现（可发能量币） |
| 综合评定 | 综合评定 | 班主任为本班学生做学期综合评定 |
| 教师成就 | 首页「教师风采」 | 拍证书/荣誉照片上传（jpg/png，≤10MB，填名称、级别等），全校同事可见；可按教师筛选查看，自己的可随时删除 |

---

## 6. 我的

- **教师档案**：填工号、任教学科、职称等——**任教学科会参与成绩录入权限**，请如实填写；
- **我的成就**：从「教师档案」页进入，上传/管理自己的证书荣誉，记录会展示在首页「教师风采」；
- **修改密码**：随时可改；
- **服务器地址**：App 直连学校服务器，正常情况不要动；换了服务器再看交付凭据里的新地址。

---

## 7. 服务器运维（管理员）

以下操作需**登录学校服务器**执行命令（登录方式与服务器地址见交付凭据资料），命令都在服务器项目目录 `AI_school/` 下执行。不熟悉命令行的学校，联系开发方支持即可。

### 7.1 AI 服务：开通与更换 API key

平台按 OpenAI 兼容协议接入大模型（DeepSeek / 通义千问 / 智谱 / Kimi 均可），用于两件事：**班主任寄语与成长总结起草**、**荣誉证书图片识别**。未配置 key 时自动降级（寄语用规则模板起草、证书转手动录入），其他功能不受影响。

**换 key 三步**：

1. 编辑服务器项目目录里的 `docker-compose.override.yml`（首次开通：照 `docker-compose.override.yml.example` 复制一份再填），改三项配置：

   | 配置项 | 填什么 |
   |---|---|
   | `AISCHOOL_AI_BASE_URL` | 供应商接口地址，如 `https://api.deepseek.com` |
   | `AISCHOOL_AI_API_KEY` | 供应商后台申请的 key（`sk-` 开头） |
   | `AISCHOOL_AI_MODEL` | 模型名，选型见下 |

2. 重启后端生效：

   ```bash
   docker compose -f docker-compose.yml -f docker-compose.server.yml -f docker-compose.override.yml up -d report-server
   ```

3. 验证：用任一老师账号起草一份寄语（或上传证书识别一次），管理端「AI 用量」页签出现消耗记录即已接通。

**模型选型**：寄语/总结用纯文本模型即可（如 `deepseek-v4-flash`，便宜量大）；证书识别必须**多模态（视觉）模型**（如 `deepseek-v4-flash-vision-exp`、`qwen-vl`、`glm-4v`）。系统只配一个 model——两个功能都要用，请配多模态模型。

**费用**：key 为充值制，余额与账单在供应商官网查看；校内消耗在「AI 用量」页签按日/按教师可查。

### 7.2 数据备份与恢复

- 服务器已配置**每日自动备份**（凌晨执行、保留 14 天），覆盖 MySQL 全库 + 全部照片/PDF 文件，存于项目 `backups/` 目录。
- 恢复涉及导入 SQL 与还原文件，建议联系开发方或在支持指导下进行。
- 建议每学期末把当期备份拷贝到校外存储（U盘/网盘）做双保险。

### 7.3 整站打不开时的自检

1. 先分清「整站打不开」还是「个别功能报错」——个别功能多为网络波动，刷新即可，或查第 8 章常见问题；
2. 登录服务器执行 `docker compose ps`：五个容器应全部 **Up (healthy)**；有 Exited 的先 `docker compose up -d` 拉起，仍不行则记下容器名与时间反馈开发方；
3. 只有 App 连不上、电脑浏览器正常：查手机网络和 App「我的 → 服务器地址」。

### 7.4 磁盘空间

报告 PDF 与照片随日常使用累积（报告「重新生成」会保留历史版本）。服务器磁盘将满时优先清理 MinIO 中旧版本报告，或扩容磁盘；重要数据可先用各列表页的「导出」留档。

### 7.5 版本更新

在服务器项目目录执行（数据库结构变更会自动迁移，数据不受影响；建议由开发方执行或在指导下操作）：

```bash
git pull
docker compose -f docker-compose.yml -f docker-compose.server.yml -f docker-compose.override.yml up -d --build
```

（未配置 AI、服务器没有 override 文件时，去掉命令里最后一个 `-f` 项）

---

## 8. 常见问题

- **登录时提示「用户名或密码错误」**：核对账号密码；新密码注意大小写。
- **提示「尝试过于频繁」**：密码连错 5 次锁 10 分钟，稍等后再试。
- **提示「连接服务器失败」**：先确认手机网络；再核对「我的 → 服务器地址」是否正确。
- **App 里看不到 PDF/下载没反应**：App 走系统分享面板——选查看器看，选「保存到文件」存手机。
- **老师看不到某个班**：班主任看本班，任课教师看任课班；找管理员核实班级指定/任课关系。
- **整个网站/App 都打不开**：按 7.3 自检服务器容器状态。
- **AI 功能提示「AI 未配置」**：学校尚未开通 AI 服务，不影响其他功能；开通与更换 key 的方法见 7.1。
